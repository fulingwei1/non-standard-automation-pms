# 环境变量配置文件（Docker Compose 使用）

# 数据库配置
DB_ROOT_PASSWORD=root_password_change_me
DB_PASSWORD=pms_password_change_me
SECRET_KEY=$(openssl rand -base64 32)

# Grafana 配置
GRAFANA_PASSWORD=admin_change_me

# Redis 配置
REDIS_URL=redis://redis:6379/0

# 应用配置
DEBUG=false
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# 数据加密配置（P1高优先级）
# 生成密钥: python scripts/generate_encryption_key.py
# ⚠️ 密钥一旦丢失，已加密数据将无法解密！请妥善保管！
DATA_ENCRYPTION_KEY=  # 请使用 generate_encryption_key.py 生成

# API速率限制配置（可选）
RATE_LIMIT_ENABLED=true
# RATE_LIMIT_STORAGE_URL=redis://redis:6379/1  # 使用独立的Redis数据库，未设置则使用REDIS_URL
RATE_LIMIT_DEFAULT=100/minute
RATE_LIMIT_LOGIN=5/minute
RATE_LIMIT_REGISTER=3/hour
RATE_LIMIT_REFRESH=10/minute
RATE_LIMIT_PASSWORD_CHANGE=3/hour
RATE_LIMIT_DELETE=20/minute
RATE_LIMIT_BATCH=10/minute

# 邮件配置（可选）
EMAIL_ENABLED=false
EMAIL_SMTP_SERVER=smtp.example.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=noreply@example.com
EMAIL_PASSWORD=smtp_password

# 企业微信配置（可选）
WECHAT_ENABLED=false
WECHAT_CORP_ID=your_corp_id
WECHAT_AGENT_ID=your_agent_id
WECHAT_SECRET=your_secret

# 短信配置（可选）
SMS_ENABLED=false
SMS_ALIYUN_ACCESS_KEY_ID=your_access_key
SMS_ALIYUN_ACCESS_KEY_SECRET=your_access_secret

# ========================================
# 售前AI系统配置
# ========================================

# 智谱 GLM-5 API（推荐，默认使用）
# 获取方式: https://open.bigmodel.cn/
# 优势: Agentic Coding能力，200K上下文，思考模式，国内稳定
ZHIPU_API_KEY=your_zhipu_api_key_here

# OpenAI GPT-4 API（备用）
# 获取方式: https://platform.openai.com/
OPENAI_API_KEY=your_openai_api_key_here

# Kimi API（备用）
# 获取方式: https://platform.moonshot.cn/
KIMI_API_KEY=your_kimi_api_key_here

# AI模型选择（可选，默认: glm-5）
# 可选值: glm-5, gpt-4, kimi
DEFAULT_AI_MODEL=glm-5

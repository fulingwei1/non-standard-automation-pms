# ========================================
# SECRET_KEY 配置示例
# ========================================
# 
# 这个文件展示了如何配置 SECRET_KEY 和旧密钥
# 
# 重要提示:
# 1. 永远不要提交真实的密钥到 Git
# 2. 生产环境必须设置 SECRET_KEY
# 3. 使用 'python scripts/manage_secrets.py generate' 生成安全密钥
# 4. 密钥长度必须 ≥ 32 字符
#
# ========================================

# ========================================
# 方式1: 环境变量（推荐用于开发环境）
# ========================================

# 当前密钥（必需）
SECRET_KEY=your-generated-secret-key-here-at-least-32-chars

# 旧密钥列表（可选，用于密钥轮转后的向后兼容）
# 多个密钥用逗号分隔，最多保留3个
# OLD_SECRET_KEYS=old-key-1,old-key-2,old-key-3

# ========================================
# 方式2: Docker Secrets（推荐用于生产环境）
# ========================================

# 密钥文件路径（优先级高于 SECRET_KEY 环境变量）
# SECRET_KEY_FILE=/run/secrets/secret_key

# 旧密钥文件路径
# OLD_SECRET_KEYS_FILE=/run/secrets/old_secret_keys

# ========================================
# 密钥管理配置（可选）
# ========================================

# 密钥最小长度（字符数），默认32
# SECRET_KEY_MIN_LENGTH=32

# 推荐的密钥轮转周期（天），默认90
# SECRET_KEY_ROTATION_DAYS=90

# 旧密钥有效期（天），默认30
# OLD_KEYS_GRACE_PERIOD_DAYS=30

# 最多保留的旧密钥数量，默认3
# OLD_KEYS_MAX_COUNT=3

# ========================================
# 生成新密钥
# ========================================
# 
# 使用以下命令生成安全密钥:
# 
# python scripts/manage_secrets.py generate
# 
# 或使用 Python:
# 
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# 
# ========================================

# ========================================
# 密钥轮转流程
# ========================================
# 
# 1. 生成新密钥:
#    python scripts/manage_secrets.py rotate
# 
# 2. 更新环境变量:
#    SECRET_KEY=<新密钥>
#    OLD_SECRET_KEYS=<旧密钥>,<更旧的密钥>
# 
# 3. 重启应用
# 
# 4. 旧Token仍可使用30天（grace period）
# 
# 5. 通知用户重新登录以获取新Token
# 
# ========================================

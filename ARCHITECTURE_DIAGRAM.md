# 工程师进度管理系统 - 架构图解

## 系统架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                          前端层（待开发）                              │
├─────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │ 工程师工作台  │  │  PM审批中心   │  │ 跨部门看板   │              │
│  │              │  │              │  │              │              │
│  │ - 我的项目   │  │ - 待审批任务  │  │ - 部门进度   │              │
│  │ - 任务列表   │  │ - 审批历史   │  │ - 人员进度   │              │
│  │ - 进度更新   │  │ - 批量审批   │  │ - 延期监控   │              │
│  │ - 证明上传   │  │              │  │ - 阶段分析   │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ HTTP + JWT
┌─────────────────────────────────────────────────────────────────────┐
│                          API层（已完成）                               │
├─────────────────────────────────────────────────────────────────────┤
│                     FastAPI Application                              │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │              /api/v1/engineers (16个端点)                      │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │                                                                │ │
│  │  工程师端 (9个)         │  PM审批端 (4个)   │  查询端 (3个)    │ │
│  │  ─────────────          │  ──────────────   │  ────────────    │ │
│  │  • GET  /my-projects    │  • GET  /pending  │  • GET  /tasks  │ │
│  │  • POST /tasks          │  • PUT  /approve  │  • GET  /{id}   │ │
│  │  • PUT  /tasks/{id}     │  • PUT  /reject   │  • GET  /prog   │ │
│  │  • PUT  /progress       │  • GET  /history  │      -visibility│ │
│  │  • PUT  /complete       │                   │                  │ │
│  │  • POST /report-delay   │                   │                  │ │
│  │  • POST /upload-proof   │                   │                  │ │
│  │  • GET  /proofs         │                   │                  │ │
│  │  • DELETE /proof/{id}   │                   │                  │ │
│  │                                                                │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                              ↓                                       │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                   认证与权限中间件                              │ │
│  │  • JWT Token验证      • 用户身份识别     • 权限检查           │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        业务逻辑层（已完成）                            │
├─────────────────────────────────────────────────────────────────────┤
│  ┌────────────────────┐  ┌──────────────────────┐                  │
│  │  engineers.py      │  │ progress_aggregation │                  │
│  │  (API端点实现)      │  │ _service.py          │                  │
│  │                    │  │                      │                  │
│  │ • 参数验证         │  │ • 进度聚合算法       │                  │
│  │ • 业务逻辑         │  │ • 健康度计算         │                  │
│  │ • 权限控制         │  │ • 实时触发机制       │                  │
│  │ • 错误处理         │  │                      │                  │
│  └────────────────────┘  └──────────────────────┘                  │
│            ↓                         ↓                               │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Pydantic Schemas                           │  │
│  │  • 请求验证 (TaskCreateRequest, ProgressUpdateRequest, ...)  │  │
│  │  • 响应序列化 (TaskResponse, MyProjectResponse, ...)         │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        数据访问层（ORM）                               │
├─────────────────────────────────────────────────────────────────────┤
│                      SQLAlchemy Models                               │
│  ┌──────────────┐  ┌──────────────────┐  ┌──────────────────────┐  │
│  │ TaskUnified  │  │ TaskApproval     │  │ TaskCompletion       │  │
│  │              │  │ Workflow         │  │ Proof                │  │
│  │ • 67字段     │  │                  │  │                      │  │
│  │ • 基础信息   │  │ • 审批流程追踪   │  │ • 证明材料管理       │  │
│  │ • 审批字段   │  │ • 提交/批准时间  │  │ • 文件元数据         │  │
│  │ • 延期字段   │  │ • 审批意见       │  │ • 5种证明类型        │  │
│  │ • 完成字段   │  │ • 任务快照       │  │                      │  │
│  └──────────────┘  └──────────────────┘  └──────────────────────┘  │
│         ↓                    ↓                       ↓               │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              关联模型（已存在）                               │  │
│  │  • Project - 项目信息                                        │  │
│  │  • ProjectStage - 阶段信息                                   │  │
│  │  • ProjectMember - 项目成员                                  │  │
│  │  • User - 用户信息（含部门）                                 │  │
│  │  • ExceptionEvent - 异常事件                                 │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                          数据存储层                                   │
├─────────────────────────────────────────────────────────────────────┤
│  ┌────────────────────┐          ┌─────────────────────────────┐   │
│  │  SQLite (开发)      │          │  MySQL (生产)                │   │
│  │  data/app.db       │          │  云数据库/本地MySQL          │   │
│  └────────────────────┘          └─────────────────────────────┘   │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                  文件存储                                     │  │
│  │  uploads/task_proofs/{task_id}/{timestamp}_{uuid}_{filename} │  │
│  │  • 证明文档  • 照片  • 视频  • 测试报告  • 数据文件         │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 核心业务流程图

### 1. 智能任务创建流程

```
工程师创建任务
       ↓
  选择任务重要性
       ↓
    ┌─────────┐
    │判断重要性│
    └─────────┘
       ↓
   ┌───┴───┐
   ↓       ↓
IMPORTANT  GENERAL
   ↓       ↓
   ↓       ↓─────→ status = ACCEPTED (直接可执行)
   ↓
   ├→ approval_required = True
   ├→ status = PENDING_APPROVAL
   ├→ 创建 TaskApprovalWorkflow
   ├→ approver_id = 项目PM
   └→ 发送通知给PM
       ↓
    PM审批
       ↓
   ┌───┴───┐
   ↓       ↓
 批准    拒绝
   ↓       ↓
   ↓       ↓─────→ status = CANCELLED
   ↓
   ↓─────→ status = ACCEPTED
           ↓
        可以开始执行
```

### 2. 进度更新与聚合流程

```
工程师更新任务进度
(PUT /tasks/{id}/progress)
       ↓
   更新任务字段
   • progress = 50
   • actual_hours = 20.5
   • progress_note = "..."
       ↓
    状态自动转换
   ┌──────────────┐
   │ progress > 0 │ → status: ACCEPTED → IN_PROGRESS
   │ progress=100 │ → status: * → COMPLETED
   └──────────────┘
       ↓
   db.commit()
       ↓
┌──────────────────────────────────┐
│ aggregate_task_progress(task_id) │
└──────────────────────────────────┘
       ↓
   ┌────────────────────────────────┐
   │ 1. 聚合到项目进度               │
   │   • 查询项目所有任务           │
   │   • 计算加权平均：             │
   │     Σ(task.progress) / count   │
   │   • 更新 Project.progress_pct  │
   └────────────────────────────────┘
       ↓
   ┌────────────────────────────────┐
   │ 2. 聚合到阶段进度（如有）      │
   │   • 查询阶段所有任务           │
   │   • 计算加权平均               │
   │   • 更新 ProjectStage.progress │
   └────────────────────────────────┘
       ↓
   ┌────────────────────────────────┐
   │ 3. 更新项目健康度               │
   │   • 统计延期任务数量           │
   │   • 统计逾期任务数量           │
   │   • 计算延期率和逾期率         │
   │   • 判断健康度：               │
   │     - H1: 延期<10%, 逾期<5%    │
   │     - H2: 延期10-25%, 逾期5-15%│
   │     - H3: 延期>25%, 逾期>15%   │
   └────────────────────────────────┘
       ↓
    返回聚合结果
    {
      project_progress_updated: true,
      stage_progress_updated: true,
      new_project_progress: 47.5,
      new_stage_progress: 52.3
    }
```

### 3. 任务完成流程

```
工程师点击"完成任务"
       ↓
┌──────────────────────────┐
│ 验证是否有完成证明材料    │
│ (proof_count >= 1)       │
└──────────────────────────┘
       ↓
   ┌───┴───┐
   ↓       ↓
  有证明  无证明
   ↓       ↓
   ↓       └→ skip_proof_validation = true? ─┐
   ↓                  ↓                      ↓
   ↓                 Yes                    No
   ↓                  ↓                      ↓
   ↓                  ↓                   报错提示
   ↓                  ↓                   需要上传证明
   ↓←─────────────────┘
   ↓
更新任务状态
• status = COMPLETED
• progress = 100
• actual_end_date = today
• completion_note = "..."
       ↓
    触发进度聚合
    （同上进度更新流程）
       ↓
    返回完成结果
```

### 4. 延期报告流程

```
工程师发现任务无法按时完成
       ↓
填写延期报告
(POST /tasks/{id}/report-delay)
• delay_reason: "客户需求变更..."
• delay_responsibility: "客户"
• delay_impact_scope: PROJECT
• schedule_impact_days: 5
• cost_impact: 8000
• new_completion_date: 2026-01-20
       ↓
┌──────────────────────────┐
│ 1. 更新任务延期状态       │
│   • is_delayed = True    │
│   • delay_reason = ...   │
│   • new_completion_date  │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 2. 创建异常事件记录       │
│   • ExceptionEvent       │
│   • event_type = DELAY   │
│   • severity 基于影响范围 │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 3. 确定通知对象           │
│   • PM (必须)            │
│   • 部门经理 (PROJECT级)  │
│   • 相关项目经理 (MULTI)  │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 4. 发送通知（待实现）     │
│   • 邮件通知             │
│   • 企业微信通知          │
│   • 系统内通知            │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 5. 更新项目健康度         │
│   • 重新计算延期率        │
│   • 可能降低健康度等级    │
│   • H1 → H2 或 H2 → H3   │
└──────────────────────────┘
       ↓
    返回延期报告结果
    {
      exception_event_id: 89,
      notifications_sent_count: 5,
      health_status_updated: true
    }
```

### 5. PM审批流程

```
PM查看待审批任务列表
(GET /tasks/pending-approval)
       ↓
   选择一个任务
       ↓
    审阅任务详情
   • 任务标题和描述
   • 必要性说明 (justification)
   • 预估工时
   • 计划时间
       ↓
    做出审批决定
       ↓
   ┌───┴───┐
   ↓       ↓
 批准    拒绝
   ↓       ↓
PUT /approve  PUT /reject
   ↓       ↓
   ↓       填写拒绝原因
   ↓       (rejection_reason)
   ↓       ↓
   ↓       ↓
   └───┬───┘
       ↓
┌──────────────────────────┐
│ 更新任务状态              │
│ 批准:                    │
│  • approval_status=      │
│    APPROVED              │
│  • status=ACCEPTED       │
│ 拒绝:                    │
│  • approval_status=      │
│    REJECTED              │
│  • status=CANCELLED      │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 更新审批工作流记录        │
│  • approved_by = PM_ID   │
│  • approved_at = now()   │
│  • approval_note/        │
│    rejection_reason      │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 发送通知给任务创建人      │
│ （待实现）               │
└──────────────────────────┘
       ↓
    返回审批结果
```

### 6. 跨部门进度查询流程

```
部门经理/PM访问
跨部门进度看板
(GET /projects/{id}/progress-visibility)
       ↓
┌──────────────────────────┐
│ 1. 查询项目所有任务       │
│   SELECT * FROM          │
│   task_unified           │
│   WHERE project_id = ?   │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 2. 按部门分组统计         │
│   For each task:         │
│     user = task.assignee │
│     dept = user.dept     │
│     累加部门统计          │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 3. 按人员分组统计         │
│   For each task:         │
│     member = user.name   │
│     累加人员统计          │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 4. 计算进度百分比         │
│   dept_progress =        │
│     completed/total*100  │
│   member_progress = ...  │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 5. 查询阶段进度           │
│   SELECT stage_code,     │
│          progress_pct    │
│   FROM project_stages    │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│ 6. 查询活跃延期           │
│   SELECT * FROM tasks    │
│   WHERE is_delayed=True  │
│   AND status NOT IN      │
│   (COMPLETED, CANCELLED) │
└──────────────────────────┘
       ↓
    构建响应
    {
      project_id: 1,
      overall_progress: 45.5,
      department_progress: [
        {
          department_name: "机械部",
          progress_pct: 42.5,
          members: [...]
        },
        {
          department_name: "电气部",
          progress_pct: 55.6,
          members: [...]
        }
      ],
      stage_progress: {
        "S1": {progress: 100, status: "COMPLETED"},
        "S4": {progress: 45.5, status: "IN_PROGRESS"}
      },
      active_delays: [...]
    }
       ↓
    渲染可视化看板
```

---

## 数据模型关系图

```
┌──────────────────────────────────────────────────────────────────┐
│                         核心实体关系                              │
└──────────────────────────────────────────────────────────────────┘

┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│   Project   │ 1     * │  TaskUnified │ *     1 │    User     │
│             │◆────────│              │────────◇│             │
│ • id        │         │ • id         │         │ • id        │
│ • code      │         │ • task_code  │         │ • username  │
│ • name      │         │ • title      │         │ • real_name │
│ • progress  │         │ • status     │         │ • department│
│ • health    │         │ • progress   │         │             │
└─────────────┘         │ • assignee_id│         └─────────────┘
       ↓                │              │                ↑
       │ 1              │ [扩展17字段] │                │
       │                │              │                │
       │ *              │ • approval_* │                │
┌─────────────┐         │ • delay_*    │         ┌─────────────┐
│ProjectStage │         │ • completion_│         │ProjectMember│
│             │         │   note       │         │             │
│ • stage_code│         │              │         │ • project_id│
│ • progress  │         └──────────────┘         │ • user_id   │
│ • status    │                ↓                 │ • roles[]   │
└─────────────┘                │                 │ • allocation│
                               │                 └─────────────┘
                               │ 1
                        ┌──────┴──────┐
                        ↓             ↓
              ┌──────────────┐ ┌──────────────┐
              │TaskApproval  │ │TaskCompletion│
              │Workflow      │ │Proof         │
              │              │ │              │
              │• workflow_id │ │• proof_id    │
              │• task_id     │ │• task_id     │
              │• submitted_by│ │• proof_type  │
              │• approver_id │ │• file_path   │
              │• status      │ │• file_name   │
              │• approved_at │ │• uploaded_by │
              │• note        │ │• description │
              └──────────────┘ └──────────────┘
```

---

## 进度聚合算法可视化

```
┌────────────────────────────────────────────────────────────────┐
│                     三级进度聚合                                 │
└────────────────────────────────────────────────────────────────┘

任务级 (Task Level)
━━━━━━━━━━━━━━━━━━━
任务1: ████████████████████░ 80%  (机械部-张工-S4阶段)
任务2: ██████████░░░░░░░░░░ 50%  (机械部-张工-S4阶段)
任务3: ████████████████████ 100% (机械部-李工-S4阶段)
任务4: ██████░░░░░░░░░░░░░░ 30%  (电气部-王工-S4阶段)
任务5: ████████████████░░░░ 70%  (电气部-赵工-S4阶段)
任务6: ████████████████████ 100% (测试部-刘工-S5阶段)
任务7: ██████████░░░░░░░░░░ 50%  (测试部-陈工-S5阶段)
                              ↓
                          聚合算法
                         (加权平均)
                              ↓
         ┌────────────────────┴────────────────────┐
         ↓                    ↓                    ↓

阶段级 (Stage Level)
━━━━━━━━━━━━━━━━━━━
S4阶段: █████████████░░░░░░░ 65%  ← (80+50+100+30+70)/5
S5阶段: ███████████████░░░░░ 75%  ← (100+50)/2
                              ↓
                          聚合算法
                              ↓

项目级 (Project Level)
━━━━━━━━━━━━━━━━━━━
项目整体: ██████████████░░░░░ 68.6%  ← 所有任务加权平均

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

健康度计算
━━━━━━━━━━━━━━━━━━━
总任务数: 7
延期任务: 1 (14.3%)  ← 任务2
逾期任务: 0 (0%)

判断逻辑:
• 延期率 14.3% → 在 10%-25% 区间
• 逾期率 0%    → 在 <5% 区间
• 结果: H2 (有风险) 🟡
```

---

## 部署架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                        生产环境架构                                │
└──────────────────────────────────────────────────────────────────┘

                        外部网络
                           ↓
                ┌──────────────────┐
                │  Nginx / Apache  │  (反向代理 + HTTPS)
                │  Port: 443       │
                └──────────────────┘
                           ↓
                ┌──────────────────┐
                │  负载均衡器       │  (可选 - 高可用部署)
                └──────────────────┘
                           ↓
        ┌──────────────────┴──────────────────┐
        ↓                                     ↓
┌──────────────────┐              ┌──────────────────┐
│  应用服务器1      │              │  应用服务器2      │
│                  │              │                  │
│  Gunicorn        │              │  Gunicorn        │
│  • 4 workers     │              │  • 4 workers     │
│  • Uvicorn       │              │  • Uvicorn       │
│  Port: 8000      │              │  Port: 8000      │
└──────────────────┘              └──────────────────┘
        ↓                                     ↓
        └──────────────────┬──────────────────┘
                          ↓
                ┌──────────────────┐
                │  MySQL 数据库     │
                │  Port: 3306      │
                │                  │
                │  • 主从复制      │
                │  • 定时备份      │
                │  • 连接池        │
                └──────────────────┘

                ┌──────────────────┐
                │  文件存储         │
                │  uploads/        │
                │                  │
                │  • 本地存储      │
                │  • 或 OSS/S3     │
                └──────────────────┘

监控与日志
━━━━━━━━━━━━━━━━━━━
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│  应用日志         │   │  性能监控         │   │  健康检查         │
│  /var/log/       │   │  • Prometheus    │   │  /health         │
│  gunicorn/       │   │  • Grafana       │   │  每30秒检查       │
└──────────────────┘   └──────────────────┘   └──────────────────┘
```

---

## 技术栈全景图

```
┌──────────────────────────────────────────────────────────────────┐
│                      技术栈依赖关系                                │
└──────────────────────────────────────────────────────────────────┘

应用层
━━━━━━━━━━━━━━━━━━━
┌────────────────────────────────────────────────────────────┐
│  FastAPI 0.104+                                             │
│  • 路由管理  • 依赖注入  • 数据验证  • OpenAPI文档生成      │
└────────────────────────────────────────────────────────────┘
           ↓                    ↓                    ↓
    ┌──────────┐       ┌──────────────┐      ┌──────────────┐
    │ Pydantic │       │ Python-Jose  │      │ Python-Multi │
    │ 2.0+     │       │ (JWT认证)     │      │ part (文件)  │
    │ 数据验证  │       │              │      │              │
    └──────────┘       └──────────────┘      └──────────────┘

数据库层
━━━━━━━━━━━━━━━━━━━
┌────────────────────────────────────────────────────────────┐
│  SQLAlchemy 1.4+                                            │
│  • ORM映射  • 关系管理  • 查询构建  • 事务管理             │
└────────────────────────────────────────────────────────────┘
           ↓                                        ↓
    ┌──────────┐                            ┌──────────────┐
    │ SQLite   │ (开发)                      │ MySQL 8.0+   │ (生产)
    │ 3.31+    │                            │ PyMySQL      │
    └──────────┘                            └──────────────┘

安全层
━━━━━━━━━━━━━━━━━━━
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│ python-jose  │       │  passlib     │       │  bcrypt      │
│ JWT Token    │       │  密码哈希     │       │  密码加密     │
└──────────────┘       └──────────────┘       └──────────────┘

服务器层
━━━━━━━━━━━━━━━━━━━
┌────────────────────────────────────────────────────────────┐
│  Uvicorn (ASGI服务器)                                       │
│  • 异步IO  • WebSocket支持  • 高性能                        │
└────────────────────────────────────────────────────────────┘
           ↓
┌────────────────────────────────────────────────────────────┐
│  Gunicorn (进程管理器)                                      │
│  • 多进程  • 负载均衡  • 优雅重启                           │
└────────────────────────────────────────────────────────────┘
```

---

## 文件上传流程图

```
前端上传文件
       ↓
POST /tasks/{id}/completion-proofs/upload
Content-Type: multipart/form-data
       ↓
┌────────────────────────────┐
│ 1. 接收文件                 │
│   • file: UploadFile       │
│   • proof_type: str        │
│   • description: str       │
└────────────────────────────┘
       ↓
┌────────────────────────────┐
│ 2. 验证文件                 │
│   • 文件大小 < 10MB        │
│   • 文件类型合法           │
│   • 文件名安全             │
└────────────────────────────┘
       ↓
┌────────────────────────────┐
│ 3. 生成存储路径             │
│   uploads/task_proofs/     │
│   {task_id}/               │
│   {timestamp}_{uuid}_      │
│   {filename}               │
└────────────────────────────┘
       ↓
┌────────────────────────────┐
│ 4. 保存文件到磁盘           │
│   with open(path, 'wb'):   │
│     file.save()            │
└────────────────────────────┘
       ↓
┌────────────────────────────┐
│ 5. 创建数据库记录           │
│   TaskCompletionProof(     │
│     task_id=task_id,       │
│     file_path=path,        │
│     file_name=filename,    │
│     file_size=size,        │
│     ...                    │
│   )                        │
└────────────────────────────┘
       ↓
┌────────────────────────────┐
│ 6. 返回证明信息             │
│   {                        │
│     id: 45,                │
│     file_name: "...",      │
│     uploaded_at: "..."     │
│   }                        │
└────────────────────────────┘
       ↓
    前端显示上传成功
```

---

## 权限控制流程图

```
客户端请求 + JWT Token
       ↓
┌────────────────────────────┐
│ 1. JWT中间件                │
│   • 提取Token               │
│   • 验证签名                │
│   • 检查过期时间            │
└────────────────────────────┘
       ↓
   Token有效？
       ↓
   ┌───┴───┐
   ↓       ↓
  有效    无效
   ↓       ↓
   ↓    401 Unauthorized
   ↓
┌────────────────────────────┐
│ 2. 解码用户信息             │
│   • user_id                │
│   • username               │
│   • roles                  │
└────────────────────────────┘
       ↓
┌────────────────────────────┐
│ 3. 查询用户完整信息         │
│   db.query(User)           │
│     .filter(id=user_id)    │
└────────────────────────────┘
       ↓
   用户激活？
       ↓
   ┌───┴───┐
   ↓       ↓
 激活    未激活
   ↓       ↓
   ↓    403 Forbidden
   ↓
┌────────────────────────────┐
│ 4. 资源权限检查             │
│   (取决于端点)              │
│                            │
│ • 任务负责人验证:           │
│   task.assignee_id ==      │
│   current_user.id          │
│                            │
│ • PM权限验证:               │
│   project.pm_id ==         │
│   current_user.id          │
│                            │
│ • 项目成员验证:             │
│   user in project.members  │
└────────────────────────────┘
       ↓
   有权限？
       ↓
   ┌───┴───┐
   ↓       ↓
  有权限  无权限
   ↓       ↓
   ↓    403 Forbidden
   ↓
 执行业务逻辑
       ↓
    返回结果
```

---

## 实时通知架构（未来增强）

```
┌────────────────────────────────────────────────────────────────┐
│                      通知系统架构（规划）                         │
└────────────────────────────────────────────────────────────────┘

业务事件触发
━━━━━━━━━━━━━━━━━━━
• 任务创建（IMPORTANT）
• 审批完成
• 任务延期
• 任务完成
• 进度更新
       ↓
┌────────────────────────────┐
│ 消息队列 (Celery/RabbitMQ) │
│ • 异步处理                  │
│ • 可靠投递                  │
│ • 失败重试                  │
└────────────────────────────┘
       ↓
┌────────────────────────────┐
│ 通知分发器                  │
│ • 确定接收人                │
│ • 选择通知渠道              │
│ • 消息模板渲染              │
└────────────────────────────┘
       ↓
   ┌───┴────┬────────┬────────┐
   ↓        ↓        ↓        ↓
┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐
│邮件 │ │微信 │ │短信 │ │站内 │
│通知 │ │通知 │ │通知 │ │消息 │
└─────┘ └─────┘ └─────┘ └─────┘
   ↓        ↓        ↓        ↓
 用户收到通知
```

---

## 缓存策略架构（性能优化）

```
┌────────────────────────────────────────────────────────────────┐
│                      缓存层架构（优化方案）                        │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────┐
│ 应用层 Cache                │
│ • Python LRU Cache         │
│ • 缓存时间: 1-5分钟         │
│ • 缓存对象:                 │
│   - 项目进度聚合结果        │
│   - 跨部门进度视图          │
└────────────────────────────┘
       ↓ (未命中)
┌────────────────────────────┐
│ Redis Cache (可选)          │
│ • 分布式缓存                │
│ • TTL: 5-30分钟             │
│ • 缓存键:                   │
│   project:{id}:progress    │
│   project:{id}:visibility  │
└────────────────────────────┘
       ↓ (未命中)
┌────────────────────────────┐
│ 数据库查询                  │
│ • MySQL Query              │
│ • 使用索引优化              │
└────────────────────────────┘
       ↓
    返回结果
       ↓
    更新缓存
```

---

**文档版本：** 1.0.0
**创建日期：** 2026-01-07
**维护者：** 开发团队

**说明：** 本架构图解提供系统的可视化视图，帮助快速理解系统结构和业务流程。所有流程图和架构图均基于实际实现代码绘制。

# 前端页面层验收报告（Playwright 截图 + console 采集）

**日期**：2026-07-02
**目的**：补上方法论中"页面-API-数据库三方一致"的**页面那一方**（此前只做了 API+DB 两方）
**方式**：起前端 dev（vite 5173，代理 /api→后端 8002）+ Playwright(chromium) 无头浏览器，登录后逐页截图并采集 console 错误 / 失败 API（4xx/5xx）
**账号**：总经理 fulingwei（临时设密登录，验收后已还原）
**产出**：22 张全页截图 + `summary.json`（路径：scratchpad/pageshots/）

---

## 一、结果总览

覆盖 21 个关键页面（对齐八大业务链路 + BOM/齐套/ECN 演示）+ 登录页：

| 结果 | 数量 |
|---|---|
| 页面成功加载 | **21/21** |
| 有 console 错误 | 0 |
| 有失败 API(4xx/5xx) | 0 |
| 被踢回登录（鉴权失效） | 0 |

覆盖页面：dashboard、客户管理、报价、合同、项目中心、物料、**装配齐套(BOM)**、齐套检查、物料分析、采购、采购物料中心、生产执行、生产看板、发货单、外协订单、**ECN 中心**、审批、验收中心、缺料预警、成本、财务报表。

## 二、视觉抽查（确认真渲染，非空壳）
- **装配齐套看板**：项目总数 6、平均/阻塞齐套率、装配阶段齐套率、最近齐套分析列表、排产建议——真实渲染。
- **ECN 中心**：卡片列表渲染，**本次建的演示 ECN「ECN-260702-001 视觉检测机相机选型变更」赫然在列**（与种子 ECN 并存）→ 页面/API/DB 三方一致在演示数据上得到确认。
- **合同管理**：KPI（8 执行中/¥1618万/共 20 份）+ 列表 20 行（见下方时序说明）。

## 三、发现与说明
- **合同列表首屏时序**（非 bug）：首轮固定等待 3.5s 时，合同页 KPI 已出但**列表行未及渲染**（截图 `04_contracts.png` 列表区空白）。改用 `networkidle`+6s 复查：列表 API 返回 20 条、页面渲染 **20 行**（`04b_contracts_retry.png`）。结论为**首屏异步列表渲染较慢的截图时机问题，非缺陷**；建议列表加骨架屏/loading 态以改善首屏观感。
- 其余 20 页均在 3.5s 内完成渲染，无 console/API 报错。

## 四、结论
方法论中"**页面-API-数据库三方一致**"的页面维度已补齐验证：21 个关键页面在真实后端数据下均可正常登录访问、渲染内容、无前端报错；持久化演示数据（ECN）在 UI 可见，端到端一致。

**至此，八大业务链路 + 角色三件事（能看/能做/越权拦截 + 数据可见性读隔离）+ 否定链路 + 页面层，方法论各维度均已覆盖。**

> 复现：起后端(8002)+前端(`cd frontend && npm run dev`)，运行 scratchpad 内 `pw_page_accept.cjs`（需 `npx playwright install chromium`）。

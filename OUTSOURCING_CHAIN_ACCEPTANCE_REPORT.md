# 外协链路端到端验收报告

**链路**：外协供应商 → 外协下单 → 审批 → 进度上报 → 交付 → 来料质检 → 付款 → 成本归集
**验收方式**：真实角色(外协下单员/外协主管/财务/质检)登录 → 逐环节调真实 API → 每步核对数据库 → 正向 + 反向 → 整库还原清理
**日期**：2026-07-02
**环境**：后端 `app.main:app`@127.0.0.1:8002；DB SQLite `data/app.db`（验收前整库备份，验收后已还原，零残留）
**结果**：外协下单→审批→进度→付款→**成本归集端到端跑通**；交付/质检被代码缺陷阻断。检查项 **11/12 通过**，唯一未通过为高危缺陷 OS1。

---

## 1. 正向主链路（单据流转与三方核对）

| # | 环节 | 操作角色 | 结果 | 三方核对 |
|---|---|---|---|---|
| 1 | 外协供应商 | 外协下单员 | ✅ | `outsourcing_vendors` 落库 |
| 2 | 外协下单 | 外协下单员 | ✅ | `outsourcing_orders` 挂供应商/项目，含明细，total_amount=10000 |
| 3 | 订单审批 | 外协主管 | ✅ | status→**APPROVED** |
| 4 | 进度上报 | 外协下单员 | ✅ | 进度 50% 登记 |
| 5 | 交付登记 | 外协下单员 | ❌ **500** | UnboundLocalError（见 OS1）→ 交付/质检阻断 |
| 6 | 来料质检 | 质检员 | ⛔ | 承接交付失败，跳过 |
| 7 | 付款 | 财务 | ✅ | `outsourcing_payments` 落库（进度款 5000） |
| 8 | **成本归集** | 系统 | ✅ | **`project_costs` 外协成本记录 = 1（外协成本成功归集到项目）** |

**成本源闭环**：外协成本已确认归集到 `project_costs`。至此"采购/工单/人工/外协"四大成本源中，**采购、外协确认归集**（人工待 TS1 审批模板修复后可验，工单在生产链路已见 source_type）。

---

## 2. 越权 / 反向验收

| 用例 | 期望 | 结果 |
|---|---|---|
| N1 无 finance 权限(采购员)付款 | 403 | ✅ 403（付款受 `finance:read` 管控） |
| N2 无 outsourcing:approve(工程师)审批 | 403 | ✅ 403 |
| N3 未携带 token 访问 | 401 | ✅ 401 |

**RBAC 现状**：外协**下单/交付/进度/质检**为任意登录；**审批**(`outsourcing:*`)与**付款**(`finance:read`)有权限管控。

---

## 3. 发现问题（FINDINGS）

| 编号 | 严重度 | 环节 | 问题 | 影响 |
|---|---|---|---|---|
| **OS1** | 高 | 外协交付 | `outsourcing/deliveries.py:139-144` **缩进错误**：`vendor = db.query(...)` 被误缩进到 `if order.status not in [...]: raise` 块内（raise 之后的死代码）；合法订单路径下 `vendor` 从未赋值 → **`UnboundLocalError` → 500** | **外协交付端点对所有合法订单均崩溃**，连带来料质检不可用（外协交付/质检子链断裂）。属代码级缺陷（与 PR1/BOM1 同类） |

---

## 4. 数据清理

- 测试供应商/订单/付款，验收后从**验收前整库备份还原**。
- 还原后核对：`outsourcing_orders=3 / outsourcing_vendors=3 / users=195` 与基线一致，测试标记数据 **0 残留**，`PRAGMA foreign_key_check` 回到基线（69）。

---

## 5. 结论

外协链路**下单、审批、进度、付款、成本归集贯通良好**，外协成本正确归集到项目 `project_costs`，付款/审批权限管控到位。但**交付登记端点因缩进缺陷（`vendor` 变量未定义）对合法订单一律 500**（OS1），导致交付→质检子链断裂。

**待修复**：OS1 修正 `deliveries.py` 缩进（将 vendor 查询移出 if 块），恢复外协交付/质检。

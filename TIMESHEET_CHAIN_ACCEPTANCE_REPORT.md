# 工时链路端到端验收报告

**链路**：工时填报 → 提交审批 → 审批 → 统计/成本归集
**验收方式**：真实角色(工程师/审批人)登录 → 逐环节调真实 API → 每步核对数据库 → 正向 + 反向 → 整库还原清理
**日期**：2026-07-02
**环境**：后端 `app.main:app`@127.0.0.1:8002；DB SQLite `data/app.db`（验收前整库备份，验收后已还原，零残留）
**结果**：工时**填报正常、RBAC 完善**；提交/审批被**审批模板缺失**阻断。检查项 **7/10 通过**，未通过项均由缺陷 TS1 导致。

---

## 1. 正向主链路（单据流转与三方核对）

| # | 环节 | 结果 | 三方核对 |
|---|---|---|---|
| 1 | 工时填报 | ✅ | `timesheet` 落库，status=DRAFT，挂 project 104 与填报人；hours=8 |
| 2 | 提交审批 | ❌ **400** | 『审批模板不存在: TIMESHEET_APPROVAL』（见 TS1） |
| 3 | 审批 | ⛔ 阻断 | 无审批实例可审（承接 2） |
| 4 | 统计 | ✅/⚠️ | 统计接口可用(200)；因未审批，本工时未计入 APPROVED 统计（统计只算 APPROVED） |

---

## 2. 越权 / 反向验收（RBAC 完善 ✅）

| 用例 | 期望 | 结果 |
|---|---|---|
| N1 无 timesheet:create(仓库员)填报 | 403 | ✅ 403 |
| N2 无 timesheet:approve(工程师)审批 | 403 | ✅ 403 |
| N3 删除已提交/审批工时 | 拒绝 | ✅ 403 |
| N4 未携带 token 访问 | 401 | ✅ 401 |

**亮点**：工时模块 **RBAC 覆盖完善**（timesheet:create/read/update/delete/submit/approve 全套分权），是权限控制规范的模块之一——与生产/发货/售前的"执行段无 RBAC"形成对比。

---

## 3. 发现问题（FINDINGS）

| 编号 | 严重度 | 环节 | 问题 | 影响 |
|---|---|---|---|---|
| **TS1** | 高 | 工时审批 | 提交工时审批报『审批模板不存在: **TIMESHEET_APPROVAL**』，工时提交/审批不可用 | **第 3 个缺失的审批模板**（前有合同 F1=SALES_CONTRACT_APPROVAL、变更 ECN1=ECN_STANDARD）——确认"审批模板未随种子初始化"是**横跨合同/ECN/工时至少 3 个模块的系统性缺陷** |

> 连带：工时无法进入 APPROVED，导致工时成本无法计入项目人工成本统计（成本核对里的人工成本依赖已审批工时）。

---

## 4. 数据清理

- 测试工时记录，验收后从**验收前整库备份还原**。
- 还原后核对：`timesheet=428 / users=195` 与基线一致，测试标记数据 **0 残留**，`PRAGMA foreign_key_check` 回到基线（69）。

---

## 5. 结论

工时链路**填报环节正常、权限控制完善（RBAC 全套分权）**，但**提交/审批被审批模板 `TIMESHEET_APPROVAL` 缺失阻断**（TS1）。此为继合同(F1)、ECN(ECN1)之后**第三个**缺失的审批模板，确证审批模板初始化缺失是系统性问题。

**待修复**：TS1 初始化 `TIMESHEET_APPROVAL` 审批模板（建议与 F1/ECN1 一并统一补齐所有审批模板种子）。

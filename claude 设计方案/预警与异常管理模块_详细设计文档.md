# 预警与异常管理模块 详细设计文档

## 文档信息
- **模块名称**: 预警与异常管理模块（Alert & Exception Management）
- **版本**: v1.0
- **创建日期**: 2025-07-12
- **最后更新**: 2025-07-12
- **编写人**: System Architect
- **状态**: 初稿

---

## 1. 模块定位与目标

### 1.1 模块定位

预警与异常管理模块是非标自动化项目管理系统的风险监控中枢，负责对项目执行过程中的各类风险进行实时监控、预警推送和异常处理。通过建立统一的预警规则引擎，实现对进度延期、成本超支、质量异常、交期风险等问题的早期发现和及时响应，帮助管理层快速决策。

### 1.2 核心目标

| 目标 | 描述 |
|------|------|
| 风险早期发现 | 基于规则自动识别潜在风险，问题前置暴露 |
| 统一预警平台 | 整合各模块预警，提供统一的预警中心 |
| 分级响应机制 | 根据预警级别自动升级，确保及时响应 |
| 闭环跟踪处理 | 预警确认、处理、关闭的全流程跟踪 |
| 数据分析支持 | 预警趋势分析，支持流程持续改进 |

### 1.3 预警业务价值

```
┌─────────────────────────────────────────────────────────────────┐
│                      预警管理业务价值                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  问题前置发现                                                   │
│  └─ 将事后处理转变为事前预防，降低损失                          │
│                                                                 │
│  响应时效提升                                                   │
│  └─ 自动推送到责任人，缩短响应时间                              │
│                                                                 │
│  管理透明度                                                     │
│  └─ 风险可视化，管理层实时掌握项目健康度                        │
│                                                                 │
│  决策支持                                                       │
│  └─ 预警数据分析，识别系统性问题                                │
│                                                                 │
│  考核依据                                                       │
│  └─ 预警响应率、处理时效作为绩效指标                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 预警类型体系

### 2.1 预警分类架构

```
预警类型体系：

┌─────────────────────────────────────────────────────────────────┐
│                         预警类型体系                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                      项目维度                              │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  PROJ_DELAY      │ 项目进度延期预警                       │ │
│  │  PROJ_MILESTONE  │ 里程碑逾期预警                         │ │
│  │  PROJ_HEALTH     │ 项目健康度预警                         │ │
│  │  PROJ_RESOURCE   │ 资源冲突预警                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                      任务维度                              │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  TASK_OVERDUE    │ 任务逾期预警                           │ │
│  │  TASK_BLOCKED    │ 任务阻塞预警                           │ │
│  │  TASK_HANDOVER   │ 交付物缺失预警                         │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                      采购维度                              │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  PO_DELIVERY     │ 采购交期预警                           │ │
│  │  PO_SHORTAGE     │ 物料短缺预警                           │ │
│  │  PO_PRICE        │ 价格异常预警                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                      外协维度                              │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  OS_DELIVERY     │ 外协交期预警                           │ │
│  │  OS_QUALITY      │ 外协质量预警                           │ │
│  │  OS_PROGRESS     │ 外协进度预警                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                      成本维度                              │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  COST_OVERRUN    │ 成本超支预警                           │ │
│  │  COST_BUDGET     │ 预算异常预警                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                      质量维度                              │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  QA_INSPECTION   │ 检验不合格预警                         │ │
│  │  QA_ACCEPTANCE   │ 验收问题预警                           │ │
│  │  QA_COMPLAINT    │ 客户投诉预警                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                      变更维度                              │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │  ECN_PENDING     │ 变更待处理预警                         │ │
│  │  ECN_IMPACT      │ 变更影响预警                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 预警级别定义

| 级别 | 代码 | 颜色 | 响应时限 | 升级规则 | 说明 |
|------|------|------|----------|----------|------|
| 提示 | INFO | 蓝色 | 24小时 | - | 信息提示，无需立即处理 |
| 注意 | WARNING | 黄色 | 8小时 | 24小时未处理→橙色 | 需要关注，可能发展为问题 |
| 严重 | CRITICAL | 橙色 | 4小时 | 8小时未处理→红色 | 需要优先处理 |
| 紧急 | URGENT | 红色 | 1小时 | 2小时未处理→升级通知 | 必须立即处理 |

### 2.3 预警规则示例

```yaml
# 项目进度延期预警规则
PROJ_DELAY:
  name: "项目进度延期预警"
  description: "项目实际进度落后于计划进度时触发"
  conditions:
    - field: "schedule_deviation"  # 进度偏差（天）
      operator: ">="
      thresholds:
        WARNING: 3      # 延期3天 → 黄色
        CRITICAL: 7     # 延期7天 → 橙色
        URGENT: 14      # 延期14天 → 红色
  recipients:
    WARNING: [pm]
    CRITICAL: [pm, dept_manager]
    URGENT: [pm, dept_manager, gm]
  auto_escalate: true
  check_interval: "daily"

# 采购交期预警规则
PO_DELIVERY:
  name: "采购交期预警"
  description: "采购订单距交期临近或已超期时触发"
  conditions:
    - field: "days_to_delivery"  # 距交期天数
      operator: "<="
      thresholds:
        WARNING: 3      # 距交期3天 → 黄色
        CRITICAL: 1     # 距交期1天 → 橙色
        URGENT: 0       # 已超期 → 红色
  recipients:
    WARNING: [purchaser]
    CRITICAL: [purchaser, pu_manager]
    URGENT: [purchaser, pu_manager, pm]
  auto_escalate: true
  check_interval: "hourly"

# 成本超支预警规则
COST_OVERRUN:
  name: "成本超支预警"
  description: "项目实际成本超过预算时触发"
  conditions:
    - field: "cost_overrun_ratio"  # 超支比例
      operator: ">="
      thresholds:
        WARNING: 0.05   # 超支5% → 黄色
        CRITICAL: 0.10  # 超支10% → 橙色
        URGENT: 0.20    # 超支20% → 红色
  recipients:
    WARNING: [pm]
    CRITICAL: [pm, finance]
    URGENT: [pm, finance, gm]
  auto_escalate: true
  check_interval: "daily"
```

---

## 3. 预警处理流程

### 3.1 预警生命周期

```
预警生命周期：

  ┌──────────┐
  │ DETECTED │ 系统检测到预警条件
  └────┬─────┘
       │ 自动创建预警
       ▼
  ┌──────────┐
  │  ACTIVE  │ 预警激活，推送通知
  └────┬─────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
┌───────┐ ┌───────────┐
│IGNORED│ │ACKNOWLEDGED│ 责任人确认
└───────┘ └─────┬─────┘
                │
            ┌───┴───┐
            │       │
            ▼       ▼
       ┌────────┐  ┌────────┐
       │RESOLVED│  │ESCALATED│ 升级处理
       └───┬────┘  └────┬───┘
           │            │
           └─────┬──────┘
                 ▼
           ┌──────────┐
           │  CLOSED  │ 预警关闭
           └──────────┘
```

### 3.2 预警处理流程

```
┌──────────────────────────────────────────────────────────────────┐
│                        预警处理流程                               │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. 预警触发                                                      │
│     ├── 定时任务扫描各模块数据                                    │
│     ├── 匹配预警规则                                              │
│     └── 生成预警记录                                              │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  2. 预警通知                                                      │
│     ├── 根据级别确定通知对象                                      │
│     ├── 多渠道推送（站内/邮件/企微/短信）                        │
│     └── 记录通知发送时间                                          │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  3. 预警确认                                                      │
│     ├── 责任人查看预警详情                                        │
│     ├── 确认预警（或标记为误报）                                  │
│     └── 记录确认时间                                              │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  4. 预警处理                                                      │
│     ├── 分析根本原因                                              │
│     ├── 制定解决措施                                              │
│     ├── 执行处理动作                                              │
│     └── 记录处理过程                                              │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  5. 预警关闭                                                      │
│     ├── 确认问题已解决                                            │
│     ├── 填写处理结果                                              │
│     ├── 关闭预警                                                  │
│     └── 触发后续改进流程（如需要）                                │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  6. 自动升级（未及时处理时）                                      │
│     ├── 超时未确认 → 升级预警级别                                 │
│     ├── 通知上级管理者                                            │
│     └── 记录升级原因                                              │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

### 3.3 预警升级机制

```
预警自动升级机制：

时间线 ─────────────────────────────────────────────────────────→

  T0                T+8h              T+24h              T+48h
  │                  │                  │                  │
  │ 预警触发         │ 未确认           │ 未处理           │ 仍未处理
  │ (黄色)           │ 升级(橙色)       │ 升级(红色)       │ 升级通报
  │                  │                  │                  │
  ▼                  ▼                  ▼                  ▼
┌────┐            ┌────┐            ┌────┐            ┌────┐
│ PM │  ────→     │PM  │  ────→     │PM  │  ────→     │ PM │
│    │            │主管│            │主管│            │主管│
└────┘            └────┘            │总经理│          │总经理│
                                    └────┘            │通报│
                                                      └────┘

升级触发条件：
- 超过响应时限未确认
- 超过处理时限未关闭
- 预警条件持续恶化
```

---

## 4. 数据模型设计

### 4.1 核心实体关系

```
┌─────────────────────────────────────────────────────────────────┐
│                        实体关系图                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐     ┌────────────────┐                       │
│  │ alert_rules  │────→│    alerts      │                       │
│  │ (预警规则)   │     │   (预警记录)    │                       │
│  └──────────────┘     └───────┬────────┘                       │
│                               │                                 │
│                               │                                 │
│                               ▼                                 │
│  ┌──────────────┐     ┌────────────────┐                       │
│  │    users     │←────│alert_recipients│                       │
│  │   (用户)     │     │  (预警接收人)   │                       │
│  └──────────────┘     └────────────────┘                       │
│                                                                 │
│  ┌────────────────┐                                            │
│  │    alerts      │                                            │
│  └───────┬────────┘                                            │
│          │                                                      │
│          ▼                                                      │
│  ┌────────────────┐     ┌────────────────┐                     │
│  │alert_actions   │     │alert_escalations│                    │
│  │ (处理记录)     │     │  (升级记录)     │                     │
│  └────────────────┘     └────────────────┘                     │
│                                                                 │
│  ┌────────────────┐                                            │
│  │alert_statistics│                                            │
│  │  (预警统计)    │                                            │
│  └────────────────┘                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 数据表设计

#### 4.2.1 预警规则表（alert_rules）

```sql
CREATE TABLE alert_rules (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    rule_code           VARCHAR(50) NOT NULL UNIQUE,      -- 规则编码
    rule_name           VARCHAR(200) NOT NULL,            -- 规则名称
    rule_type           VARCHAR(50) NOT NULL,             -- 预警类型
    category            VARCHAR(50) NOT NULL,             -- 分类：PROJECT/TASK/PURCHASE等
    description         TEXT,                             -- 规则描述

    -- 规则条件（JSON格式）
    conditions          TEXT NOT NULL,                    -- 触发条件JSON

    -- 阈值配置
    threshold_warning   DECIMAL(10,2),                    -- 黄色预警阈值
    threshold_critical  DECIMAL(10,2),                    -- 橙色预警阈值
    threshold_urgent    DECIMAL(10,2),                    -- 红色预警阈值

    -- 通知配置
    recipients_warning  TEXT,                             -- 黄色预警接收人JSON
    recipients_critical TEXT,                             -- 橙色预警接收人JSON
    recipients_urgent   TEXT,                             -- 红色预警接收人JSON

    -- 时效配置
    response_timeout_warning  INTEGER DEFAULT 480,        -- 响应时限（分钟）
    response_timeout_critical INTEGER DEFAULT 240,
    response_timeout_urgent   INTEGER DEFAULT 60,

    -- 升级配置
    auto_escalate       BOOLEAN DEFAULT 1,                -- 是否自动升级
    escalate_timeout    INTEGER DEFAULT 480,              -- 升级时限（分钟）

    -- 检查配置
    check_interval      VARCHAR(20) DEFAULT 'hourly',     -- daily/hourly/realtime
    check_sql           TEXT,                             -- 检查SQL（可选）

    -- 通知渠道
    notify_channels     VARCHAR(200) DEFAULT 'system',    -- system/email/wechat/sms

    -- 状态
    is_active           BOOLEAN DEFAULT 1,                -- 是否启用
    is_system           BOOLEAN DEFAULT 0,                -- 是否系统规则

    created_by          INTEGER,
    created_at          DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at          DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE INDEX idx_alert_rules_type ON alert_rules(rule_type);
CREATE INDEX idx_alert_rules_category ON alert_rules(category);
CREATE INDEX idx_alert_rules_active ON alert_rules(is_active);
```

#### 4.2.2 预警记录表（alerts）

```sql
CREATE TABLE alerts (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    alert_no            VARCHAR(50) NOT NULL UNIQUE,      -- 预警编号
    rule_id             INTEGER NOT NULL,                 -- 规则ID
    rule_code           VARCHAR(50) NOT NULL,             -- 规则编码（冗余）

    -- 预警信息
    alert_type          VARCHAR(50) NOT NULL,             -- 预警类型
    alert_level         VARCHAR(20) NOT NULL,             -- 预警级别
    original_level      VARCHAR(20),                      -- 原始级别（升级前）
    title               VARCHAR(500) NOT NULL,            -- 预警标题
    content             TEXT,                             -- 预警内容
    suggestion          TEXT,                             -- 处理建议

    -- 关联信息
    source_module       VARCHAR(50),                      -- 来源模块
    source_type         VARCHAR(50),                      -- 来源类型
    source_id           INTEGER,                          -- 来源ID
    project_id          INTEGER,                          -- 关联项目
    project_name        VARCHAR(200),                     -- 项目名称（冗余）

    -- 预警数据
    metric_value        DECIMAL(14,4),                    -- 触发指标值
    threshold_value     DECIMAL(14,4),                    -- 阈值
    deviation           DECIMAL(14,4),                    -- 偏差

    -- 状态
    status              VARCHAR(20) DEFAULT 'ACTIVE',     -- 状态
    is_read             BOOLEAN DEFAULT 0,                -- 是否已读

    -- 时间
    triggered_at        DATETIME DEFAULT CURRENT_TIMESTAMP, -- 触发时间
    acknowledged_at     DATETIME,                         -- 确认时间
    resolved_at         DATETIME,                         -- 解决时间
    closed_at           DATETIME,                         -- 关闭时间

    -- 处理信息
    acknowledged_by     INTEGER,                          -- 确认人
    resolved_by         INTEGER,                          -- 解决人
    closed_by           INTEGER,                          -- 关闭人
    resolution          TEXT,                             -- 解决方案
    close_reason        VARCHAR(50),                      -- 关闭原因

    -- 升级信息
    escalate_count      INTEGER DEFAULT 0,                -- 升级次数
    last_escalate_at    DATETIME,                         -- 最后升级时间

    -- 响应时效
    response_time       INTEGER,                          -- 响应时长（分钟）
    resolve_time        INTEGER,                          -- 解决时长（分钟）

    created_at          DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at          DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (rule_id) REFERENCES alert_rules(id),
    FOREIGN KEY (project_id) REFERENCES projects(id),
    FOREIGN KEY (acknowledged_by) REFERENCES users(id),
    FOREIGN KEY (resolved_by) REFERENCES users(id),
    FOREIGN KEY (closed_by) REFERENCES users(id)
);

CREATE INDEX idx_alerts_no ON alerts(alert_no);
CREATE INDEX idx_alerts_type ON alerts(alert_type);
CREATE INDEX idx_alerts_level ON alerts(alert_level);
CREATE INDEX idx_alerts_status ON alerts(status);
CREATE INDEX idx_alerts_project ON alerts(project_id);
CREATE INDEX idx_alerts_triggered ON alerts(triggered_at);
CREATE INDEX idx_alerts_source ON alerts(source_module, source_type, source_id);
```

#### 4.2.3 预警接收人表（alert_recipients）

```sql
CREATE TABLE alert_recipients (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    alert_id        INTEGER NOT NULL,                     -- 预警ID
    user_id         INTEGER NOT NULL,                     -- 接收人ID
    recipient_type  VARCHAR(20) NOT NULL,                 -- PRIMARY/CC/ESCALATE

    -- 通知状态
    notified_at     DATETIME,                             -- 通知时间
    notify_channel  VARCHAR(20),                          -- 通知渠道
    notify_status   VARCHAR(20) DEFAULT 'PENDING',        -- PENDING/SENT/FAILED
    notify_result   TEXT,                                 -- 通知结果

    -- 阅读状态
    read_at         DATETIME,                             -- 阅读时间

    FOREIGN KEY (alert_id) REFERENCES alerts(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE INDEX idx_alert_recipients_alert ON alert_recipients(alert_id);
CREATE INDEX idx_alert_recipients_user ON alert_recipients(user_id);
```

#### 4.2.4 预警处理记录表（alert_actions）

```sql
CREATE TABLE alert_actions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    alert_id        INTEGER NOT NULL,                     -- 预警ID
    action_type     VARCHAR(50) NOT NULL,                 -- 动作类型
    action_content  TEXT NOT NULL,                        -- 动作内容

    -- 状态变更
    old_status      VARCHAR(20),                          -- 原状态
    new_status      VARCHAR(20),                          -- 新状态
    old_level       VARCHAR(20),                          -- 原级别
    new_level       VARCHAR(20),                          -- 新级别

    -- 附件
    attachments     TEXT,                                 -- 附件JSON

    created_by      INTEGER,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (alert_id) REFERENCES alerts(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE INDEX idx_alert_actions_alert ON alert_actions(alert_id);
CREATE INDEX idx_alert_actions_type ON alert_actions(action_type);
```

#### 4.2.5 预警升级记录表（alert_escalations）

```sql
CREATE TABLE alert_escalations (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    alert_id            INTEGER NOT NULL,                 -- 预警ID
    escalation_level    INTEGER NOT NULL,                 -- 升级层级

    -- 升级信息
    escalate_reason     VARCHAR(50) NOT NULL,             -- 升级原因
    escalate_from_level VARCHAR(20),                      -- 升级前级别
    escalate_to_level   VARCHAR(20),                      -- 升级后级别

    -- 通知对象
    escalate_to_users   TEXT,                             -- 升级通知人JSON

    escalated_at        DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (alert_id) REFERENCES alerts(id)
);

CREATE INDEX idx_alert_escalations_alert ON alert_escalations(alert_id);
```

#### 4.2.6 预警统计表（alert_statistics）

```sql
CREATE TABLE alert_statistics (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    stat_date           DATE NOT NULL,                    -- 统计日期
    stat_period         VARCHAR(20) NOT NULL,             -- DAILY/WEEKLY/MONTHLY

    -- 维度
    project_id          INTEGER,                          -- 项目ID（可选）
    department_id       INTEGER,                          -- 部门ID（可选）
    alert_type          VARCHAR(50),                      -- 预警类型（可选）

    -- 统计数据
    total_count         INTEGER DEFAULT 0,                -- 预警总数
    warning_count       INTEGER DEFAULT 0,                -- 黄色预警数
    critical_count      INTEGER DEFAULT 0,                -- 橙色预警数
    urgent_count        INTEGER DEFAULT 0,                -- 红色预警数

    -- 状态统计
    active_count        INTEGER DEFAULT 0,                -- 活跃预警数
    acknowledged_count  INTEGER DEFAULT 0,                -- 已确认数
    resolved_count      INTEGER DEFAULT 0,                -- 已解决数
    closed_count        INTEGER DEFAULT 0,                -- 已关闭数
    ignored_count       INTEGER DEFAULT 0,                -- 已忽略数

    -- 升级统计
    escalated_count     INTEGER DEFAULT 0,                -- 升级预警数

    -- 时效统计
    avg_response_time   INTEGER,                          -- 平均响应时间（分钟）
    avg_resolve_time    INTEGER,                          -- 平均解决时间（分钟）
    timeout_count       INTEGER DEFAULT 0,                -- 超时未处理数

    created_at          DATETIME DEFAULT CURRENT_TIMESTAMP,

    UNIQUE(stat_date, stat_period, project_id, department_id, alert_type)
);

CREATE INDEX idx_alert_statistics_date ON alert_statistics(stat_date);
CREATE INDEX idx_alert_statistics_project ON alert_statistics(project_id);
```

#### 4.2.7 预警订阅配置表（alert_subscriptions）

```sql
CREATE TABLE alert_subscriptions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         INTEGER NOT NULL,                     -- 用户ID
    alert_type      VARCHAR(50),                          -- 预警类型（空表示全部）
    project_id      INTEGER,                              -- 项目ID（空表示全部）

    -- 订阅配置
    min_level       VARCHAR(20) DEFAULT 'WARNING',        -- 最低接收级别
    notify_channels VARCHAR(200) DEFAULT 'system',        -- 通知渠道

    -- 免打扰时段
    quiet_start     TIME,                                 -- 免打扰开始时间
    quiet_end       TIME,                                 -- 免打扰结束时间

    is_active       BOOLEAN DEFAULT 1,                    -- 是否启用

    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (project_id) REFERENCES projects(id)
);

CREATE INDEX idx_alert_subscriptions_user ON alert_subscriptions(user_id);
CREATE INDEX idx_alert_subscriptions_type ON alert_subscriptions(alert_type);
```

---

## 5. 预警规则引擎

### 5.1 规则引擎架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        预警规则引擎                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                     调度器 (Scheduler)                   │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐             │   │
│  │  │每小时任务│  │ 每日任务 │  │实时触发器│             │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘             │   │
│  │       └──────────────┼──────────────┘                   │   │
│  └──────────────────────┼──────────────────────────────────┘   │
│                         ▼                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   规则匹配器 (Matcher)                    │   │
│  │                                                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │   │
│  │  │数据采集器   │→ │规则评估器   │→ │阈值比较器   │     │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘     │   │
│  │                                                          │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         ▼                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   预警生成器 (Generator)                  │   │
│  │                                                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │   │
│  │  │去重处理器   │→ │预警记录器   │→ │通知分发器   │     │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘     │   │
│  │                                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 规则检查逻辑

```python
class AlertRuleEngine:
    """预警规则引擎"""

    def check_project_delay_rule(self, project_id: int) -> Optional[Alert]:
        """
        检查项目进度延期规则
        """
        project = self.get_project(project_id)

        # 计算进度偏差
        planned_progress = self.calculate_planned_progress(project)
        actual_progress = self.calculate_actual_progress(project)
        deviation_days = self.calculate_deviation_days(
            planned_progress, actual_progress, project
        )

        # 匹配阈值
        rule = self.get_rule('PROJ_DELAY')
        level = self.match_threshold(deviation_days, rule)

        if level:
            return self.create_alert(
                rule=rule,
                level=level,
                title=f"项目【{project.name}】进度延期{deviation_days}天",
                content=f"计划进度: {planned_progress}%, 实际进度: {actual_progress}%",
                metric_value=deviation_days,
                source_module='PROJECT',
                source_id=project_id,
                project_id=project_id
            )
        return None

    def check_po_delivery_rule(self, po_id: int) -> Optional[Alert]:
        """
        检查采购交期规则
        """
        po = self.get_purchase_order(po_id)

        # 计算距交期天数
        days_to_delivery = (po.required_date - date.today()).days

        rule = self.get_rule('PO_DELIVERY')
        level = self.match_threshold_reverse(days_to_delivery, rule)

        if level:
            return self.create_alert(
                rule=rule,
                level=level,
                title=f"采购单【{po.order_no}】交期预警",
                content=f"要求交期: {po.required_date}, 距今{abs(days_to_delivery)}天",
                metric_value=days_to_delivery,
                source_module='PURCHASE',
                source_id=po_id,
                project_id=po.project_id
            )
        return None

    def check_cost_overrun_rule(self, project_id: int) -> Optional[Alert]:
        """
        检查成本超支规则
        """
        project = self.get_project(project_id)

        budget = project.budget_amount
        actual_cost = self.calculate_actual_cost(project_id)
        overrun_ratio = (actual_cost - budget) / budget if budget > 0 else 0

        rule = self.get_rule('COST_OVERRUN')
        level = self.match_threshold(overrun_ratio, rule)

        if level:
            overrun_amount = actual_cost - budget
            return self.create_alert(
                rule=rule,
                level=level,
                title=f"项目【{project.name}】成本超支{overrun_ratio*100:.1f}%",
                content=f"预算: ¥{budget:,.0f}, 实际: ¥{actual_cost:,.0f}, "
                        f"超支: ¥{overrun_amount:,.0f}",
                metric_value=overrun_ratio,
                source_module='PROJECT',
                source_id=project_id,
                project_id=project_id
            )
        return None

    def match_threshold(self, value: float, rule: AlertRule) -> Optional[str]:
        """
        正向阈值匹配（值越大级别越高）
        """
        if value >= rule.threshold_urgent:
            return 'URGENT'
        elif value >= rule.threshold_critical:
            return 'CRITICAL'
        elif value >= rule.threshold_warning:
            return 'WARNING'
        return None

    def match_threshold_reverse(self, value: float, rule: AlertRule) -> Optional[str]:
        """
        反向阈值匹配（值越小级别越高）
        """
        if value <= rule.threshold_urgent:
            return 'URGENT'
        elif value <= rule.threshold_critical:
            return 'CRITICAL'
        elif value <= rule.threshold_warning:
            return 'WARNING'
        return None
```

### 5.3 预警去重逻辑

```python
def should_create_alert(self, rule: AlertRule, source_id: int, level: str) -> bool:
    """
    判断是否应该创建新预警（去重逻辑）
    """
    # 查找相同来源的活跃预警
    existing = self.db.query(Alert).filter(
        Alert.rule_id == rule.id,
        Alert.source_id == source_id,
        Alert.status.in_(['ACTIVE', 'ACKNOWLEDGED'])
    ).first()

    if not existing:
        return True

    # 如果新预警级别更高，则升级现有预警
    if self.level_priority(level) > self.level_priority(existing.alert_level):
        self.upgrade_alert(existing, level)
        return False

    # 相同或更低级别，不重复创建
    return False

def level_priority(self, level: str) -> int:
    """预警级别优先级"""
    priorities = {
        'INFO': 1,
        'WARNING': 2,
        'CRITICAL': 3,
        'URGENT': 4
    }
    return priorities.get(level, 0)
```

---

## 6. API设计

### 6.1 预警管理API

```yaml
# 预警查询API
/api/v1/alerts:
  GET:
    summary: 获取预警列表
    parameters:
      - status: string[]        # 状态筛选
      - level: string[]         # 级别筛选
      - alert_type: string[]    # 类型筛选
      - project_id: integer     # 项目筛选
      - date_from: datetime
      - date_to: datetime
      - page: integer
      - page_size: integer
    responses:
      200:
        items: Alert[]
        total: integer
        statistics:
          total: integer
          by_level: object
          by_status: object

/api/v1/alerts/{id}:
  GET:
    summary: 获取预警详情
    responses:
      200:
        alert: Alert
        actions: AlertAction[]
        escalations: AlertEscalation[]

/api/v1/alerts/{id}/acknowledge:
  POST:
    summary: 确认预警
    requestBody:
      comment: string
    responses:
      200:
        success: boolean

/api/v1/alerts/{id}/resolve:
  POST:
    summary: 解决预警
    requestBody:
      resolution: string
      attachments: string[]
    responses:
      200:
        success: boolean

/api/v1/alerts/{id}/close:
  POST:
    summary: 关闭预警
    requestBody:
      close_reason: string    # RESOLVED/FALSE_ALARM/DUPLICATE/OBSOLETE
      comment: string
    responses:
      200:
        success: boolean

/api/v1/alerts/{id}/ignore:
  POST:
    summary: 忽略预警
    requestBody:
      reason: string
    responses:
      200:
        success: boolean

/api/v1/alerts/{id}/actions:
  GET:
    summary: 获取处理记录
  POST:
    summary: 添加处理记录
    requestBody:
      action_type: string
      action_content: string
      attachments: string[]
```

### 6.2 预警规则API

```yaml
# 预警规则管理API
/api/v1/alerts/rules:
  GET:
    summary: 获取规则列表
    parameters:
      - category: string
      - is_active: boolean
    responses:
      200:
        items: AlertRule[]

  POST:
    summary: 创建规则
    requestBody:
      rule_code: string
      rule_name: string
      rule_type: string
      category: string
      conditions: object
      threshold_warning: number
      threshold_critical: number
      threshold_urgent: number
      recipients_warning: string[]
      recipients_critical: string[]
      recipients_urgent: string[]
      check_interval: string
      notify_channels: string[]
    responses:
      201:
        rule_id: integer

/api/v1/alerts/rules/{id}:
  GET:
    summary: 获取规则详情
  PUT:
    summary: 更新规则
  DELETE:
    summary: 删除规则

/api/v1/alerts/rules/{id}/toggle:
  POST:
    summary: 启用/禁用规则
```

### 6.3 预警统计API

```yaml
# 预警统计API
/api/v1/alerts/statistics:
  GET:
    summary: 获取预警统计
    parameters:
      - period: string          # DAILY/WEEKLY/MONTHLY
      - date_from: date
      - date_to: date
      - project_id: integer
      - department_id: integer
    responses:
      200:
        summary:
          total: integer
          by_level: object
          by_type: object
          by_status: object
        trends: object[]
        top_projects: object[]
        response_metrics:
          avg_response_time: integer
          avg_resolve_time: integer
          timeout_rate: number

/api/v1/alerts/statistics/dashboard:
  GET:
    summary: 预警仪表盘数据
    responses:
      200:
        active_alerts:
          total: integer
          urgent: integer
          critical: integer
          warning: integer
        today_new: integer
        today_closed: integer
        overdue_alerts: integer
        recent_alerts: Alert[]
```

### 6.4 订阅配置API

```yaml
# 预警订阅API
/api/v1/alerts/subscriptions:
  GET:
    summary: 获取我的订阅配置
  PUT:
    summary: 更新订阅配置
    requestBody:
      subscriptions:
        - alert_type: string
          project_id: integer
          min_level: string
          notify_channels: string[]
          quiet_start: time
          quiet_end: time
          is_active: boolean
```

---

## 7. UI设计

### 7.1 预警中心首页

```
┌─────────────────────────────────────────────────────────────────┐
│  预警中心                                           [规则配置]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      预警概览                            │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │🔴 紧急  │ │🟠 严重  │ │🟡 注意  │ │📊 今日  │       │   │
│  │  │   3    │ │   8    │ │   15   │ │处理 12 │       │   │
│  │  │  +2↑   │ │  +1↑   │ │  -3↓   │ │关闭 8  │       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ [全部] [紧急] [严重] [注意] [待处理] [已确认] [已关闭]   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 🔍 搜索预警  [项目▼] [类型▼] [日期范围▼] [🔍]            │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 🔴 AL-2501-0023  项目进度延期预警              10分钟前   │ │
│  │    项目【XX自动化产线】进度延期15天                       │ │
│  │    计划进度: 80%  实际进度: 52%                           │ │
│  │    负责人: 张三(PM)                   [确认] [查看详情]   │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 🔴 AL-2501-0022  采购交期预警                  30分钟前   │ │
│  │    采购单【PO-2501-0015】已超期2天                       │ │
│  │    供应商: XX五金厂  物料: 轴承(10件)                     │ │
│  │    负责人: 李四(采购)                 [确认] [查看详情]   │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 🟠 AL-2501-0021  成本超支预警                   2小时前   │ │
│  │    项目【YY检测设备】成本超支12.5%                       │ │
│  │    预算: ¥500,000  实际: ¥562,500  超支: ¥62,500         │ │
│  │    ✓ 张三已确认(1小时前)              [处理] [查看详情]  │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 🟡 AL-2501-0020  外协进度预警                   3小时前   │ │
│  │    外协单【OC-2501-0010】进度落后，仅完成60%             │ │
│  │    要求交期: 01-12  当前进度: 60%                         │ │
│  │    负责人: 王五(采购)                 [确认] [查看详情]   │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  共 26 条预警                                 < 1 2 3 ... 3 >   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 预警详情页面

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 返回   预警详情  AL-2501-0023                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🔴 紧急 │ 项目进度延期预警                               │   │
│  │ ─────────────────────────────────────────────────────── │   │
│  │ 项目【XX自动化产线项目】进度严重延期                     │   │
│  │                                                          │   │
│  │ 触发时间: 2025-01-10 09:30:15                           │   │
│  │ 预警规则: PROJ_DELAY (项目进度延期预警)                  │   │
│  │ 当前状态: 待处理                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 预警指标                                                 │   │
│  │ ─────────────────────────────────────────────────────── │   │
│  │ 计划进度:    80%                                         │   │
│  │ 实际进度:    52%                                         │   │
│  │ 进度偏差:    28%                                         │   │
│  │ 延期天数:    15天                                        │   │
│  │ 预警阈值:    14天 (红色)                                 │   │
│  │                                                          │   │
│  │ ████████████████░░░░░░░░░░░░░░░░░░░░ 52%                │   │
│  │ ░░░░░░░░░░░░░░░░░░░░░░░░░░████████████ 80% (计划)       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 处理建议                                                 │   │
│  │ ─────────────────────────────────────────────────────── │   │
│  │ 1. 立即召开项目进度专题会议，分析延期原因                │   │
│  │ 2. 识别关键路径任务，调配资源加快进度                    │   │
│  │ 3. 与客户沟通，评估是否需要调整交付时间                  │   │
│  │ 4. 制定详细的赶工计划并每日跟踪                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 通知记录                                                 │   │
│  │ ─────────────────────────────────────────────────────── │   │
│  │ 09:30 系统通知 → 张三(PM)        ✓ 已送达               │   │
│  │ 09:30 企业微信 → 张三(PM)        ✓ 已送达               │   │
│  │ 09:32 系统通知 → 王主管          ✓ 已送达               │   │
│  │ 09:35 系统通知 → 李总            ✓ 已送达               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 处理记录                                                 │   │
│  │ ─────────────────────────────────────────────────────── │   │
│  │ (暂无处理记录)                                           │   │
│  │                                                          │   │
│  │ [+ 添加处理记录]                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │     [忽略预警]    [确认预警]    [标记已解决]    [关闭]   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.3 预警规则配置

```
┌─────────────────────────────────────────────────────────────────┐
│  预警规则配置                                     [+ 新建规则]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ [全部] [项目类] [任务类] [采购类] [外协类] [成本类] [质量] │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 规则名称          类型      检查频率   状态    操作       │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 项目进度延期预警   PROJ     每日      ● 启用   [编辑]     │ │
│  │ └─ 阈值: 黄色≥3天 橙色≥7天 红色≥14天                      │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 采购交期预警       PO       每小时    ● 启用   [编辑]     │ │
│  │ └─ 阈值: 黄色≤3天 橙色≤1天 红色≤0天                       │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 外协交期预警       OS       每小时    ● 启用   [编辑]     │ │
│  │ └─ 阈值: 黄色≤3天 橙色≤1天 红色≤0天                       │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 成本超支预警       COST     每日      ● 启用   [编辑]     │ │
│  │ └─ 阈值: 黄色≥5% 橙色≥10% 红色≥20%                        │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 物料短缺预警       PO       每日      ○ 禁用   [编辑]     │ │
│  │ └─ 阈值: 齐套率黄色<80% 橙色<60% 红色<40%                  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.4 规则编辑弹窗

```
┌─────────────────────────────────────────────────────────────────┐
│  编辑预警规则                                            [×]    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 基本信息                                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 规则编码:  [PROJ_DELAY                            ]     │   │
│  │ 规则名称:  [项目进度延期预警                      ]     │   │
│  │ 规则分类:  [项目类                               ▼]     │   │
│  │ 规则描述:  [项目实际进度落后于计划进度时触发     ]     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 阈值配置                                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 检查指标:  [进度延期天数                         ▼]     │   │
│  │                                                          │   │
│  │ 🟡 黄色预警:  延期 ≥ [3    ] 天                         │   │
│  │ 🟠 橙色预警:  延期 ≥ [7    ] 天                         │   │
│  │ 🔴 红色预警:  延期 ≥ [14   ] 天                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 通知配置                                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 🟡 黄色通知:  [PM                                  ▼]   │   │
│  │ 🟠 橙色通知:  [PM, 部门主管                        ▼]   │   │
│  │ 🔴 红色通知:  [PM, 部门主管, 总经理                ▼]   │   │
│  │                                                          │   │
│  │ 通知渠道:  ☑ 站内消息  ☑ 企业微信  ☐ 邮件  ☐ 短信    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 升级配置                                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ ☑ 启用自动升级                                          │   │
│  │                                                          │   │
│  │ 🟡 黄色响应时限:  [8    ] 小时  超时升级为橙色          │   │
│  │ 🟠 橙色响应时限:  [4    ] 小时  超时升级为红色          │   │
│  │ 🔴 红色响应时限:  [1    ] 小时  超时通知上级            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 检查配置                                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 检查频率:  ○ 实时  ○ 每小时  ● 每日                     │   │
│  │ 启用状态:  ● 启用  ○ 禁用                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    [取消]        [保存]                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.5 预警统计仪表盘

```
┌─────────────────────────────────────────────────────────────────┐
│  预警统计分析                              [本周▼] [导出报表]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      预警趋势图                          │   │
│  │                                                          │   │
│  │  40│    ╭╮                                               │   │
│  │    │   ╱  ╲   ╭─╮                                       │   │
│  │  30│  ╱    ╲ ╱   ╲                                      │   │
│  │    │ ╱      ╳     ╲    ╭─╮                              │   │
│  │  20│╱      ╱ ╲     ╲  ╱   ╲                             │   │
│  │    │      ╱   ╲     ╲╱     ╲                            │   │
│  │  10│─────────────────────────────                       │   │
│  │    └──────────────────────────────────────              │   │
│  │      周一  周二  周三  周四  周五  周六  周日             │   │
│  │                                                          │   │
│  │      ── 新增预警  ── 已处理  ── 累计活跃               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────┐  ┌──────────────────────┐          │
│  │ 预警级别分布          │  │ 预警类型分布          │          │
│  │                       │  │                       │          │
│  │   ┌─────┐            │  │ 项目进度  ████████ 35%│          │
│  │   │ 🔴 │ 15%        │  │ 采购交期  █████ 22%   │          │
│  │   │ 🟠 │ 30%        │  │ 外协进度  ████ 18%    │          │
│  │   │ 🟡 │ 55%        │  │ 成本超支  ███ 15%     │          │
│  │   └─────┘            │  │ 其他      ██ 10%      │          │
│  └──────────────────────┘  └──────────────────────┘          │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 响应时效统计                                             │   │
│  │ ─────────────────────────────────────────────────────── │   │
│  │ 平均响应时间:  2.5小时    平均解决时间:  8.3小时        │   │
│  │ 响应达标率:    85%        处理达标率:    78%            │   │
│  │ 超时未处理:    5个        本周升级:      3次            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 预警高发项目 TOP5                                        │   │
│  │ ─────────────────────────────────────────────────────── │   │
│  │ 1. XX自动化产线项目    12个预警  (进度:5 成本:4 其他:3) │   │
│  │ 2. YY检测设备项目       8个预警  (进度:3 采购:3 其他:2) │   │
│  │ 3. ZZ包装线项目         6个预警  (外协:4 质量:2)        │   │
│  │ 4. AA组装线项目         4个预警  (进度:2 成本:2)        │   │
│  │ 5. BB测试台项目         3个预警  (采购:2 外协:1)        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 8. 权限设计

### 8.1 功能权限矩阵

| 功能 | 普通用户 | PM | 部门主管 | 管理员 |
|------|:--------:|:--:|:--------:|:------:|
| 查看预警列表 | ○ | ✓ | ✓ | ✓ |
| 查看预警详情 | ○ | ✓ | ✓ | ✓ |
| 确认预警 | ○ | ✓ | ✓ | ✓ |
| 处理预警 | ○ | ✓ | ✓ | ✓ |
| 关闭预警 | ✗ | ✓ | ✓ | ✓ |
| 忽略预警 | ✗ | ○ | ✓ | ✓ |
| 查看统计 | ✗ | ✓ | ✓ | ✓ |
| 配置规则 | ✗ | ✗ | ✗ | ✓ |
| 配置订阅 | ✓ | ✓ | ✓ | ✓ |

> ○ 表示仅限本人相关数据

### 8.2 数据权限

| 角色 | 数据范围 |
|------|----------|
| 普通用户 | 本人接收的预警 |
| PM | 本项目所有预警 |
| 部门主管 | 本部门所有预警 |
| 管理员 | 全部预警 |

---

## 9. 集成与通知

### 9.1 各模块预警集成点

```
┌─────────────────────────────────────────────────────────────────┐
│                      预警模块集成点                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  项目管理模块                                                   │
│  ├── 项目进度偏差检查点                                         │
│  ├── 里程碑完成检查点                                           │
│  └── 项目健康度检查点                                           │
│                                                                 │
│  任务管理模块                                                   │
│  ├── 任务逾期检查点                                             │
│  ├── 任务阻塞检查点                                             │
│  └── 交付物缺失检查点                                           │
│                                                                 │
│  采购管理模块                                                   │
│  ├── 采购订单交期检查点                                         │
│  ├── 物料齐套率检查点                                           │
│  └── 采购价格异常检查点                                         │
│                                                                 │
│  外协管理模块                                                   │
│  ├── 外协交期检查点                                             │
│  ├── 外协进度检查点                                             │
│  └── 外协质量检查点                                             │
│                                                                 │
│  成本管理模块                                                   │
│  ├── 成本超支检查点                                             │
│  └── 预算执行检查点                                             │
│                                                                 │
│  验收管理模块                                                   │
│  ├── 验收问题检查点                                             │
│  └── 验收逾期检查点                                             │
│                                                                 │
│  变更管理模块                                                   │
│  ├── 变更待处理检查点                                           │
│  └── 变更影响评估检查点                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 9.2 通知渠道配置

```yaml
notification_channels:
  system:
    name: "站内消息"
    description: "系统内消息中心"
    config:
      store_in_db: true
      show_badge: true

  wechat_work:
    name: "企业微信"
    description: "企业微信消息推送"
    config:
      corp_id: "${WECHAT_CORP_ID}"
      agent_id: "${WECHAT_AGENT_ID}"
      secret: "${WECHAT_SECRET}"
      template:
        urgent: "【紧急预警】{title}\n{content}\n请立即处理"
        critical: "【严重预警】{title}\n{content}"
        warning: "【预警通知】{title}\n{content}"

  email:
    name: "邮件"
    description: "邮件通知"
    config:
      smtp_host: "${SMTP_HOST}"
      smtp_port: 587
      username: "${SMTP_USER}"
      password: "${SMTP_PASS}"
      from_address: "alert@company.com"
      from_name: "项目管理系统预警"

  sms:
    name: "短信"
    description: "短信通知（仅紧急预警）"
    config:
      provider: "aliyun"
      access_key: "${SMS_ACCESS_KEY}"
      secret_key: "${SMS_SECRET_KEY}"
      sign_name: "项目管理"
      template_code: "SMS_123456"
```

---

## 10. 配置项

### 10.1 系统配置

| 配置项 | 配置键 | 默认值 | 说明 |
|--------|--------|--------|------|
| 预警检查间隔(小时) | alert.check_interval.hourly | 1 | 小时级检查间隔 |
| 预警检查间隔(每日) | alert.check_interval.daily | "08:00" | 每日检查时间 |
| 默认响应时限(黄) | alert.response_timeout.warning | 480 | 分钟 |
| 默认响应时限(橙) | alert.response_timeout.critical | 240 | 分钟 |
| 默认响应时限(红) | alert.response_timeout.urgent | 60 | 分钟 |
| 自动升级开关 | alert.auto_escalate.enabled | true | - |
| 预警保留天数 | alert.retention_days | 365 | 天 |
| 统计数据保留 | alert.statistics_retention_days | 730 | 天 |

### 10.2 预设规则配置

```yaml
preset_rules:
  - code: PROJ_DELAY
    name: "项目进度延期预警"
    category: PROJECT
    thresholds: [3, 7, 14]
    check_interval: daily
    enabled: true

  - code: PROJ_MILESTONE
    name: "里程碑逾期预警"
    category: PROJECT
    thresholds: [1, 3, 7]
    check_interval: daily
    enabled: true

  - code: TASK_OVERDUE
    name: "任务逾期预警"
    category: TASK
    thresholds: [1, 3, 5]
    check_interval: daily
    enabled: true

  - code: PO_DELIVERY
    name: "采购交期预警"
    category: PURCHASE
    thresholds: [3, 1, 0]
    check_interval: hourly
    enabled: true

  - code: PO_SHORTAGE
    name: "物料短缺预警"
    category: PURCHASE
    thresholds: [80, 60, 40]
    check_interval: daily
    enabled: true

  - code: OS_DELIVERY
    name: "外协交期预警"
    category: OUTSOURCING
    thresholds: [3, 1, 0]
    check_interval: hourly
    enabled: true

  - code: COST_OVERRUN
    name: "成本超支预警"
    category: COST
    thresholds: [5, 10, 20]
    check_interval: daily
    enabled: true

  - code: QA_REJECTION
    name: "检验不合格预警"
    category: QUALITY
    thresholds: [5, 10, 20]
    check_interval: realtime
    enabled: true
```

---

## 11. 附录

### 11.1 预警状态对照

| 状态码 | 中文名称 | 英文名称 | 说明 |
|--------|----------|----------|------|
| ACTIVE | 激活 | Active | 预警已触发，待处理 |
| ACKNOWLEDGED | 已确认 | Acknowledged | 责任人已确认 |
| RESOLVED | 已解决 | Resolved | 问题已解决 |
| CLOSED | 已关闭 | Closed | 预警已关闭 |
| IGNORED | 已忽略 | Ignored | 预警被忽略 |
| ESCALATED | 已升级 | Escalated | 预警已升级 |

### 11.2 预警关闭原因

| 原因码 | 中文名称 | 说明 |
|--------|----------|------|
| RESOLVED | 问题已解决 | 预警对应的问题已处理完成 |
| FALSE_ALARM | 误报 | 预警为误报，实际不存在问题 |
| DUPLICATE | 重复 | 与其他预警重复 |
| OBSOLETE | 已过时 | 预警已不再适用 |

### 11.3 动作类型定义

| 动作类型 | 说明 |
|----------|------|
| CREATE | 创建预警 |
| ACKNOWLEDGE | 确认预警 |
| COMMENT | 添加备注 |
| ESCALATE | 升级预警 |
| ASSIGN | 指派处理人 |
| RESOLVE | 解决预警 |
| CLOSE | 关闭预警 |
| REOPEN | 重新打开 |
| IGNORE | 忽略预警 |

---

## 版本历史

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| v1.0 | 2025-07-12 | System Architect | 初始版本 |

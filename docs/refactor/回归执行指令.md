# Phase 0 回归执行指令

```bash
cd /Users/flw/.openclaw/workspace/non-standard-automation-pms

# 1) 启动回归（严格模式）
python scripts/startup_regression_check.py --strict true --enable-stub false | tee startup-strict.log

# 2) 启动回归（非严格模式）
python scripts/startup_regression_check.py --strict false --enable-stub false | tee startup-nonstrict.log

# 3) 对严格模式失败项做分层建议
python scripts/classify_router_failures.py startup-strict.log

# 4) 本地执行两类 CI 守卫脚本
python scripts/ci_guard_stub_defaults.py
python scripts/ci_guard_router_strict.py
```

> 若 strict 模式失败，请把 `startup-strict.log` 发给我，我会直接给出“必须修复 / 可临时降级”定稿名单。

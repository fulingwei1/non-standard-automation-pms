# 交付验收链路端到端验收报告

**链路**：建验收单 → 阶段门校验 → 开始验收 → 验收项判定 → 验收问题(登记→解决→验证→关闭) → 提交审批 → 完成 → 签署
**验收方式**：真实角色(QA/验收负责人/工程师)登录 → 逐环节调真实 API → 每步核对数据库 → 正向 + 反向 → 整库还原清理
**日期**：2026-07-02
**环境**：后端 `app.main:app`@127.0.0.1:8002；DB SQLite `data/app.db`（验收前整库备份，验收后已还原，零残留）
**结果**：交付验收主链路**端到端跑通**。检查项 **13/14 通过**，唯一未通过为已确认缺陷（建单/执行无 RBAC）。

---

## 1. 正向主链路（单据流转与三方核对）

| # | 环节 | 结果 | 三方核对 |
|---|---|---|---|
| 0 | 阶段门前置 | ✅ | **FAT 验收要求 `machine.stage` 达 S5(调试完成)**，否则 400（有效阶段门控制） |
| 1 | 建验收单(FAT) | ✅ | `acceptance_orders` 挂 project 104，status DRAFT |
| 2 | 开始验收 | ✅ | status→**IN_PROGRESS**（要求验收单有检查项，`total_items>0`） |
| 3 | 验收项判定 | ✅ | 检查项 result_status→PASS |
| 4 | 验收问题登记 | ✅ | `acceptance_issues` 落库 |
| 4 | 验收问题闭环 | ✅ | 登记→解决→验证→**CLOSED** 全生命周期 |
| 5 | 提交(+审批) | ✅ | submit 成功（审批为独立可选流） |
| 6 | 完成验收 | ✅ | status→**COMPLETED**，overall_result=PASS |
| 7 | 签署(完成后) | ✅ | `acceptance_signatures` 入库；**签署要求验收单已 COMPLETED**（有效前置） |

**已验证的业务控制**：
- FAT **阶段门**：设备(machine.stage)须达 S5 才能发起 FAT 验收；
- **开始验收**要求验收单存在检查项(total_items>0)；
- **完成验收**要求必填检查项均已判定(validate_required_check_items)；
- **签署**要求验收单已完成(COMPLETED)；
- 验收**问题状态机**：未解决不能直接验证(400)。

---

## 2. 越权 / 反向验收

| 用例 | 期望 | 结果 |
|---|---|---|
| N1 无关角色(仓库员)建验收单 | 应 403 | ❌ **201 未拦截**（见 ACC1） |
| N2 验收问题未解决直接验证 | 拒绝 | ✅ 400（状态机） |
| N3 未携带 token 访问 | 401 | ✅ 401 |

---

## 3. 发现问题（FINDINGS）

| 编号 | 严重度 | 环节 | 问题 | 影响 |
|---|---|---|---|---|
| **ACC1** | 中 | 验收鉴权 | `acceptance-orders` 的建单/开始/签署/完成等端点仅 `get_current_active_user`；仅**模板/审批**端点挂 `acceptance:*`。任意登录用户(实测仓库员)可建验收单并推进至完成 | 交付验收缺职责分离（与生产/发货/售前同类的执行段无 RBAC） |

> 说明：验收模块 RBAC 覆盖不均——模板管理与审批有权限，核心单据流转无权限。

---

## 4. 数据清理

- 测试验收单/机台/验收项/问题/签署，及临时阶段变更(project/machine→S5)，验收后从**验收前整库备份还原**。
- 还原后核对：`acceptance_orders=3 / machines=3 / users=195`、项目104 stage 回到 S1，测试标记数据 **0 残留**，`PRAGMA foreign_key_check` 回到基线（69）。

---

## 5. 结论

交付验收链路**业务主干完整可靠**：从建验收单、阶段门校验、开始、验收项判定、验收问题闭环(登记→解决→验证→关闭)、完成到签署全流程状态流转正确，且**多重业务控制有效**（FAT 阶段门、检查项前置、完成前置、签署前置、问题状态机）。是目前**业务规则控制最完善**的链路之一。

**待修复**：ACC1 验收单建单/执行缺 RBAC（中）。

# 物料齐套率链路端到端验收报告

**链路**：齐套率计算 → 缺料上报/确认/处理/解决 → 替代料(技术审批→生产审批→执行) → 到货登记/收货 → 缺料检测/预警
**验收方式**：真实角色登录 → 逐环节调真实 API → 每步核对数据库 → 正向 + 反向(状态机) → 整库还原清理
**日期**：2026-07-02
**环境**：后端 `app.main:app`@127.0.0.1:8002；DB SQLite `data/app.db`（验收前整库备份，验收后已还原，零残留）
**结果**：缺料处理与替代料双审**端到端跑通**；齐套率计算受"无已发布BOM"数据缺口阻断。检查项 **16/17 通过**。

---

## 1. 正向主链路（单据流转与三方核对）

| 段 | 环节 | 结果 | 三方核对 |
|---|---|---|---|
| A | 齐套率计算 | ⚠️ | 接口正常响应(非500)；项目无『已发布BOM』返 404（见观察 KR-obs） |
| A | 缺料分析齐套率 | ✅ | `/shortage/analytics/kit-rate` 200 |
| B | 缺料上报 | ✅ | `shortage_reports` 入库，状态 REPORTED，shortage_qty=4 |
| B | 确认→处理→解决 | ✅ | REPORTED→CONFIRMED→HANDLING→**RESOLVED** 状态机正确流转 |
| C | 替代料申请 | ✅ | `material_substitutions` 入库，状态 DRAFT |
| C | 技术审批 | ✅ | DRAFT→**PROD_PENDING** |
| C | 生产审批 | ✅ | PROD_PENDING→**APPROVED** |
| C | 执行替代料 | ✅ | APPROVED→**EXECUTED** |
| D | 到货登记→收货 | ✅ | `material_arrivals` 入库并收货 |
| E | 缺料检测+预警 | ✅ | inventory-warnings/check 与 detection/alerts 均 200，不崩溃 |

**亮点**：替代料是标准**双重审批状态机**（技术审批 + 生产审批 + 执行），流转严格且可核对；缺料单四态流转完整。

---

## 2. 越权 / 反向验收

| 用例 | 期望 | 结果 |
|---|---|---|
| N1 跳过技术审批直接生产审批 | 拒绝 | ✅ 400（"只有草稿状态才能技术审批 / 只有待生产审批才能生产审批"） |
| N2 替代料未审批直接执行 | 拒绝 | ✅ 400（状态机拦截） |
| N3 未携带 token 访问 | 401 | ✅ 401 |
| N4 无关角色(销售员)处理缺料单 | 应 403 | ❌ 非 403（无权限校验，见 KR1） |

**状态机控制完善**：替代料双审顺序、执行前置均以 400 严格拦截。

---

## 3. 发现问题（FINDINGS）

| 编号 | 严重度 | 环节 | 问题 | 影响 |
|---|---|---|---|---|
| **KR1** | 中 | 齐套率/缺料鉴权 | `kit-rate` 与 `shortage/handling`（缺料上报/处理/替代料双审/到货）端点均仅 `get_current_active_user`，**无细粒度 RBAC** | 缺料处理与替代料审批缺职责分离（状态机有效但权限开放，任意登录可上报/处理/审批） |
| **KR-obs** | 观察 | 齐套率计算 | 齐套率计算要求项目有『已发布BOM』，但全库 `bom_headers` 无 PUBLISHED 状态（仅 APPROVED/空），所有项目 `kit-rate` 返 404/400 | 齐套率功能在现有数据下无法产出结果；需确认 BOM 发布流程是否缺失或"已发布"状态口径不一致 |

---

## 4. 数据清理

- 测试缺料单/替代料/到货单，验收后从**验收前整库备份还原**。
- 还原后核对：`shortage_reports=3 / material_substitutions=6 / users=195` 与基线一致，测试标记数据 **0 残留**，`PRAGMA foreign_key_check` 回到基线（69）。

---

## 5. 结论

物料齐套率链路的**缺料处理与替代料双重审批业务闭环完整可靠**：缺料单四态流转、替代料"技术→生产→执行"双审状态机、到货收货、缺料检测预警均正常，状态机对越序操作严格拦截。

**待修复/待确认**：KR1 齐套率/缺料链路缺 RBAC（中，与生产/发货同类）；KR-obs 齐套率计算依赖的『已发布BOM』在现有数据中不存在，需确认 BOM 发布流程与状态口径（建议补一条已发布BOM后复验齐套率数值）。

# 生产链路端到端验收报告

**链路**：项目/生产计划 → 工单 → 报工 → 质检/异常 → 完工
**验收方式**：真实角色(生产计划员/车间工人/质检员)登录 → 逐环节调真实 API → 每步核对数据库 → 正向 + 反向 → 整库还原清理
**日期**：2026-07-02
**环境**：后端 `app.main:app`@127.0.0.1:8002；DB SQLite `data/app.db`（验收前整库备份，验收后已还原，零残留）
**结果**：链路主干 **端到端跑通**；检查项 **14/16 通过**，2 项未通过均为**已确认的高危缺陷**（质检模块崩溃、生产链路无 RBAC）。

---

## 1. 测试账号与角色

| 用户 | 角色 | 说明 |
|---|---|---|
| acc_planner | 验收-生产计划员 | 建计划/工单、派工、审批报工 |
| acc_worker | 验收-车间工人 | 报工（对应 `worker` 表工人记录，非直接 users.id） |
| acc_qc | 验收-质检员 | 质检登记 |
| acc_sales_rep | 验收-销售员 | 无关角色越权对照 |

> 说明：派工 `assigned_to` 与报工 `worker_id` 引用的是 **`worker` 表主键**（非 `users.id`），验收为此创建了对应工人记录。

---

## 2. 正向主链路（单据流转与三方核对）

代表性一次完整执行（单据号，均已在验收后还原清理）：

| # | 环节 | 操作角色 | 单据 | 状态流转 | 三方核对 |
|---|---|---|---|---|---|
| 1 | 生产计划 | 计划员 | 计划（id 82） | DRAFT | ✅ DB 存在，带 project_id=104 |
| 2 | 计划提交/审批/发布 | 计划员 | — | DRAFT→SUBMITTED→APPROVED→**PUBLISHED** | ✅ DB status=PUBLISHED |
| 3 | 建工单 | 计划员 | 工单（id 53） | PENDING | ✅ 挂 production_plan_id=82，plan_qty=10 |
| 4 | 派工+启动 | 计划员 | — | PENDING→**STARTED** | ✅ assigned_to=工人id |
| 5 | 工人报工 | 车间工人 | 报工（id 4） | 完工10件 → 审批 APPROVED | ✅ DB completed_qty=10, qualified_qty=10 |
| 6 | 工单完工 | 计划员 | — | →**COMPLETED** | ✅ completed_qty=10, progress=100 |
| 7 | 质检 | 质检员 | — | ❌ **400** | **质检模块崩溃**（见发现 PR1） |
| 8 | 生产异常 | 工人/计划员 | 异常（id 29） | 登记→处理→**RESOLVED** | ✅ 状态流转正常 |

**关键规则验证**：工单挂生产计划、派工启动、报工数量入库、报工审批、工单完工进度=100%、异常闭环——均正确。

---

## 3. 越权 / 反向验收

| 用例 | 期望 | 结果 |
|---|---|---|
| N1 报工数量>工单计划数(10) | 拒绝 | ✅ 400（报工数量校验生效） |
| N2 质检合格数>送检数 | 拒绝 | ⚠️ 400（但因质检模块崩溃 PR1 提前报错，**该校验未能真正验证**） |
| N3 无关角色(销售员)审批生产计划 | 应 403 | ❌ **200 未拦截**（见发现 PR2） |
| N4 未携带 token 访问 | 401 | ✅ 401 |

---

## 4. 发现问题（FINDINGS）

| 编号 | 严重度 | 环节 | 问题 | 影响 |
|---|---|---|---|---|
| **PR1** | 高 | 生产质检模块 | `app/api/v1/endpoints/production/quality.py` 多处（63/159/219/287 行）使用 `current_user["id"]`，而 `current_user` 是 `User` ORM 对象 → `'User' object is not subscriptable`。涉及 **质检登记 / 缺陷分析 / 质检预警规则 / 返工** 全部写接口 | 生产质检写入口**全部不可用**；且质检数量校验无法生效（请求在校验前即崩溃） |
| **PR2** | 高 | 生产链路鉴权 | 生产端点普遍仅 `get_current_active_user`/`get_current_user`，**无任何细粒度 RBAC**；无关角色(销售员)可审批生产计划、可完成工单等 | 生产计划/工单/报工/完工审批缺职责分离与越权防护 |

> 修复建议：PR1 将 `current_user["id"]` 统一改为 `current_user.id`（一处模式错误，全模块波及）；PR2 为生产端点补 `production:*` 权限校验（create/approve/report 分权）。

---

## 5. 数据清理

- 本链路创建的全部测试数据（生产计划/工单/报工/异常/测试工人）与测试用户/角色，验收后从**验收前整库备份还原**。
- 还原后核对：`production_plan / work_order / worker / users` 行数与基线一致，测试标记数据 **0 残留**，`PRAGMA foreign_key_check` 回到基线（69）。

---

## 6. 结论

生产链路**业务主干端到端跑通**：一条生产任务从计划走到工单、派工、报工、完工，工单进度正确累计到 100%，异常可闭环；报工数量校验有效。

**待修复**：PR1 质检模块 `current_user["id"]` 代码缺陷导致质检写接口全崩（高，且拖累质检数量校验）；PR2 生产链路缺 RBAC（高）。

# 产品路线图（10x 价值版）

> 生成日期：2026-07-03。基于全代码库勘察（后端 ~2,861 路由 / 267 服务、前端 ~517 路由 / 400+ 页面）、
> `AI_FEATURE_BACKLOG.md`、`BUG_SWEEP_REPORT.md`、各链路验收报告与 `APPROVALS_SCOPE_ASSESSMENT.md` 综合提炼。
> 北极星原则（承接 AI_FEATURE_BACKLOG）：**让人少填，数据自动带出 / AI 获得；先打通干净、按 ID 关联的经营数据，再上层 AI。**
> 针对非标两大痛点：**需求不清晰致失败**、**非标亏损 / 返工**。

## 现状一句话诊断

广度业内罕见（销售→售前→项目→生产→售后全链路 + 11 状态机 + 统一审批引擎），但**深度不均**：
核心链路（销售/项目/生产/ECN）已达生产级，而**库存模型缺失、通知渠道半残、AI 报价闭环未成环、
前端全局搜索和通知铃铛是死占位符、无实时层、无飞书集成**。10x 价值不在"再加一个模块"，
而在：① 把数据底座做实（可信），② 把 AI 报价/检索闭环做成（差异化壁垒），③ 把信息触达做通（实时 + 飞书）。

## 优先级总览

| 阶段 | 主题 | 特性 | 预估 |
|------|------|------|------|
| P0 | 可信底座（先修地基） | F1 库存台账真实化 · F2 审批越权收口 · F3 通知闭环与调度器持久化 | 3-4 周 |
| P1 | 差异化壁垒（最大价值） | F4 RAG 语义检索 · F5 AI 报价成本闭环 · F6 销售预测真数据 | 5-7 周 |
| P2 | 信息触达与效率 | F7 全局搜索/命令面板 · F8 实时通知层 + 飞书集成 | 3-4 周 |
| P3 | 一线与交付物 | F9 车间移动端 PWA 离线化 · F10 导出能力补齐 | 3-4 周 |
| P4 | 规模化与平台债 | F11 多租户完整化 · F12 前端数据层统一 | 持续 |

---

## P0 可信底座

### F1 库存/物料台账真实化 ⭐ 治本项

**问题**：`shortage/smart_alert_engine.py:16` FIXME 引用不存在的 `Inventory` 模型；
`material_transfer_service.py` 引用不存在的 `ProjectMaterial`（BUG_SWEEP §四已建议单独立项）。
缺料预警是 MES 核心卖点，但底层无真实库存数据，预警不可信 → 全链条（齐套率/领料/采购建议）都是空转。

**范围**：
- 新建 `Inventory`/`StockTransaction` 模型（入库/出库/调拨/盘点四类事务，按物料+库位+批次），走 alembic 迁移（sqlite+mysql 双份）。
- 收货（GoodsReceipt）、领料（MaterialRequisition）、调拨（重写 material_transfer_service）三个写入口接入事务流水。
- smart_alert_engine 改读真实库存；齐套率（kit_rate）计算切换数据源。

**验收标准**：
1. 创建采购收货 → 库存数量增加，且 `StockTransaction` 留痕可查（API 断言 + 单测）。
2. 车间领料审批通过 → 库存扣减；库存不足时领料单被拒且返回明确错误码（非 500）。
3. 人为把某 BOM 物料库存降到需求量以下 → 24h 内（或手动触发 job）生成缺料预警，且预警内容含缺口数量。
4. 齐套率看板数字 = 手工按库存/BOM 计算结果（写一个对账测试脚本自动比对）。
5. `material_transfer_service` 全部单测不再依赖 `sys.modules` 桩替换。

### F2 审批可见性与权限收口 ⭐ 安全 P0

**问题**（APPROVALS_SCOPE_ASSESSMENT §三，未见修复记录）：`GET /approvals`、`GET /approvals/{id}`、
`by-entity` 等端点零可见性过滤，任何登录用户可看全公司审批（含薪资/合同金额）；
`remind/add-cc/comments/terminate` 缺参与人校验。另：发票端点仅 `finance:read` 即可开票/作废/收款
（SALES_CHAIN 报告）；采购申请审批缺权限校验（PROCUREMENT_CHAIN D1）。

**范围**：按评估文档 Sprint 4/5 方案落地 `check_instance_visible`（参与人可见性：发起人/审批人/代理人/抄送人）、
新增 `approval:admin` 角色、抄送人脱敏（不下发 `form_data`）、发票端点拆分 `finance:write`/`invoice:void` 权限码、
补 D1。遵循既有原则"先授权、后设卡、防锁死"（每个权限码含 gm+admin）。

**验收标准**：
1. 非参与人调用 `GET /approvals/{id}` 返回 403/404；参与人正常返回（覆盖 4 类参与人各写一条测试）。
2. `GET /approvals` 列表仅返回当前用户参与的实例；`approval:admin` 角色可见全部并可终止异常流程。
3. 抄送人取详情时响应体无 `form_data` 字段（脱敏断言）。
4. 仅有 `finance:read` 的用户开票/作废/收款均 403；授予新权限码后成功。
5. 全量跑 `tests/api` 审批相关用例零回归；`PERMISSION_COVERAGE_AUDIT.json` 重新生成，越权项清零。

### F3 通知闭环与调度器持久化

**问题**：SMS 渠道是假成功桩（`channels/sms_handler.py:31` 直接返回 success 不发送）；
预警默认接收人解析未实现（`alert_subscription_service.py:139`）；项目变更请求的团队/审批通知 TODO
（`project_change_requests/service.py:115,239`）；APScheduler 用 `MemoryJobStore`，重启后 54 个定时任务的
运行状态丢失。**预警发得出去、到得了人，是 F1 缺料预警产生价值的前提。**

**范围**：SMS 未配置时显式降级为失败并落库（不再假成功）；补齐预警默认接收人（按角色/部门解析）；
补项目变更通知；APScheduler 切 `SQLAlchemyJobStore` + 启动时恢复。

**验收标准**：
1. `SMS_ENABLED=false` 时发送记录状态为 `skipped/failed` 而非 `success`（数据库断言）。
2. 无订阅人的预警规则触发时，按规则配置的默认角色解析出 ≥1 个接收人并产生系统通知。
3. 提交项目变更请求 → 项目成员与审批人各收到一条通知（集成测试）。
4. 重启后端进程后 `GET /scheduler` 显示全部任务存在且 next_run_time 正确；杀进程期间错过的关键任务有补跑或告警。

---

## P1 差异化壁垒（AI 闭环）

### F4 相似案例/方案/BOM 语义检索（RAG）⭐ AI Backlog 唯一未完成的 P0

**价值**：非标报价靠"整机拍脑袋"，历史项目的方案/BOM/成本是最值钱的数据资产但检索不到。
RAG 是报价类比、技术评审、复盘知识图谱（backlog #21）的共同使能项。

**范围**：
- 嵌入层：接现有 `ai_client_service`（百炼 text-embedding），向量存储起步用 SQLite+numpy 或 sqlite-vss，
  预留 pgvector/Milvus 接口（勿过度设计）。
- 索引对象：历史项目（方案文档+复盘）、报价单（含成本结构）、BOM（模块级）、踩坑记录（pitfalls）。
- API：`POST /api/v1/knowledge/similar` 支持按文本/按项目 ID 找相似，返回相似度+关键差异摘要。
- 前端：报价页、技术评审页嵌"相似案例"侧栏；接入 CommandBar。

**验收标准**：
1. 用 20 个真实历史项目构造评测集（人工标注每个项目的 top-3 相似项目），检索 top-5 命中率 ≥ 70%。
2. 新建报价单填入需求描述后，侧栏 3 秒内返回相似历史报价及其毛利率/最终成本。
3. 索引增量更新：新项目结项后触发索引，1 小时内可被检索到（定时任务 + 集成测试）。
4. AI 不可用（key 失效）时页面降级为普通关键词检索，不阻塞主流程。

### F5 AI 报价成本闭环（模块化 BOM + 反向校准）⭐ 直击"非标亏损"

**价值**：backlog 明确的主线——报价从"整机拍脑袋"到"模块累加 + 历史类比 + 风险加成 + 实际成本反向校准"。
M4（模块 BOM 自动拼装）、M6（参数化）、"报价→实际成本反向校准"三项均未完成，且依赖 F4。

**范围**：
- M4：按设备类型（ICT/FCT/EOL/烧录/老化/视觉）维护模块库，报价时选模块自动拼装 BOM 草稿。
- M6：模块支持参数（工位数/节拍/精度），参数驱动 BOM 数量与价格系数。
- 校准环：项目结项时自动比对报价成本 vs 实际归集成本（`project_costs`，注意 PROCUREMENT_CHAIN D2 的
  归集时点问题需一并确认），偏差率写回模块库作为下次报价的风险加成依据。

**验收标准**：
1. 选择"FCT 测试设备 + 4 工位"模板 → 自动生成 BOM 草稿，覆盖该类设备历史 BOM 平均物料数的 ≥ 80%。
2. 修改参数（工位 4→6）→ 相关模块数量/价格联动更新（快照测试）。
3. 任一结项项目详情页可见"报价 vs 实际"偏差表，分模块展示；偏差率计算有对账单测。
4. 连续报 3 个同类项目，第 3 次报价建议值与实际成本的偏差 < 前两次（用回放历史数据的仿真测试证明校准环生效）。
5. 报价审批流展示 AI 建议值与人工值的差异，留痕入 `ApprovalRecord`。

### F6 销售预测切真数据

**问题**：`sales_forecast_service.py:167/329/402` 三处 TODO——目标、历史、大单输入全是硬编码占位，
预测和漏斗分析数字不可信，管理层看板形同虚设。

**范围**：目标接 `sales_targets` 真表；历史成交按合同/回款真数据聚合；大单从商机表按金额阈值取。

**验收标准**：
1. 代码中三处 TODO 清除，`grep -n "TODO" sales_forecast_service.py` 为空。
2. 预测接口返回值与手工 SQL 聚合结果一致（对账测试，容差 ±1%）。
3. 无数据的新租户/新周期返回空态结构而非假数字。

---

## P2 信息触达与效率

### F7 全局搜索 + 命令面板

**问题**：Header 搜索框（`Header.jsx:96-113`）`onClick={() => {}}` 是死占位；`components/ai/CommandBar.jsx`
已存在但未接线；~517 条路由全靠 14 组侧边栏人肉找，功能可发现性是全系统最大的日常摩擦。

**范围**：⌘K 唤起 CommandBar；统一搜索 API（项目/机台/客户/合同/物料/审批/页面路由 7 类实体，
项目编码 PJ*/机台 PN* 前缀识别直达）；权限过滤复用 data_scope；F4 完成后接语义检索作为兜底。

**验收标准**：
1. 任意页面 ⌘K 唤起，输入项目编码 `PJ250708001` 回车直达项目详情（e2e 用例）。
2. 搜索返回结果按实体类型分组，仅含当前用户数据权限内的记录（用受限角色账号断言查不到越权项目）。
3. 搜索接口 P95 < 500ms（本地 1 万项目量级数据下压测）。
4. Header 死占位符、死的"个人信息/账户设置"菜单项（`Header.jsx:159-166`）全部接通或移除。

### F8 实时通知层 + 飞书集成

**问题**：全系统零 WebSocket/SSE，通知铃铛（`Header.jsx:119-129`）无 onClick、红点是写死的；
面向中国市场的 MES/PM 系统却没有飞书/钉钉渠道（webhook_handler 仅企微，飞书只出现在注释里）。
缺料/审批/预警是分钟级时效业务，靠人工刷新等于没有。

**范围**：
- 后端 SSE 端点（比 WebSocket 简单且够用）推送系统通知；铃铛接 NotificationCenter，实时红点计数。
- 飞书渠道 handler：审批待办卡片（可直接同意/拒绝回调）、预警推送、@责任人。复用统一通知服务的渠道抽象。
- 审批超时提醒走飞书加急。

**验收标准**：
1. A 用户提交审批 → B 用户浏览器 5 秒内铃铛红点 +1，点开可见该条通知（双浏览器 e2e）。
2. 配置飞书 webhook 后触发缺料预警 → 飞书群 30 秒内收到卡片，含物料名/缺口/责任人。
3. 飞书审批卡片点"同意"→ 系统内审批实例状态流转，留痕与系统内操作一致。
4. SSE 断线自动重连，断线期间的通知在重连后补齐（不丢消息）。

---

## P3 一线与交付物

### F9 车间移动端 PWA 离线化

**问题**：`src/pages/mobile/` 已有 9 个车间页面（扫码开工/报工/缺料上报等），但无 Service Worker、
无离线队列、主应用无响应式——车间 WiFi 覆盖差是常态，扫码报工失败 = 工人放弃使用 = 生产数据断供。

**范围**：mobile 路由注册 PWA（manifest + SW）；写操作走 IndexedDB 离线队列，恢复网络后自动重放
（带幂等键防重复报工）；只读数据（我的任务/工单）本地缓存。

**验收标准**：
1. 断网状态下提交报工 → UI 提示"已暂存"，恢复网络 30 秒内自动上传成功且不重复（幂等测试：同一幂等键重放服务端仅落一条）。
2. 断网可打开"我的任务"页并看到最后一次缓存数据，带缓存时间戳。
3. Lighthouse PWA 审计 mobile 路由 ≥ 90 分。
4. 队列重放冲突（工单已被他人关闭）时给出明确冲突提示而非静默失败。

### F10 导出能力补齐

**问题**：售前 AI 的 PDF/Word/Excel 导出全是 TODO 桩（`presale_ai_export_service.py:46/66/71`）；
`report_data_generation/router.py:89` 部分报表类型返回"待实现"；CHANGELOG 计划中的"高级报表导出"。
对客户交付物（方案书/报价书）导不出来，AI 生成的内容就出不了系统。

**范围**：售前方案书/报价书 PDF+Word 导出（复用已有 reportlab/python-docx + ppt_generator 的模板思路，
含公司抬头与水印服务）；盘点"待实现"报表类型逐一补齐或从前端下架。

**验收标准**：
1. 售前方案页点导出 → 得到含封面/目录/方案正文/报价表的 PDF 与 Word，中文无乱码（黄金文件快照比对）。
2. 导出文件带水印（复用 watermark_service），审计日志留痕谁在何时导出。
3. `grep -rn "待实现" app/services/report_data_generation/` 为空——每个报表类型要么可用要么前端不再展示入口。

---

## P4 规模化与平台债（持续推进）

### F11 多租户完整化（SaaS 前提）

**问题**：CHANGELOG 1.0.0 愿景是 10–10,000 租户 SaaS，但核心 `Project` 模型无 `tenant_id`
（`tenant_service.py:249`），租户隔离对最核心的数据不成立。**在做实前不得对外宣传多租户。**

**验收标准**：
1. `Project` 及其子表（成本/成员/里程碑）全部带 `tenant_id` 且迁移脚本可在存量库执行（含回滚脚本）。
2. 租户 A 用户以任何 API（含报表/搜索/导出）均取不到租户 B 的项目数据——写一组跨租户越权矩阵测试全绿。
3. 租户中间件对无 tenant 上下文的请求默认拒绝（fail-closed 测试）。

### F12 前端数据层与 UI 统一（技术债，随功能顺带做）

**问题**：react-query/zustand 已装未用，116 个页面手写 axios（无共享缓存/统一失效）；AntD 6 与
Radix/Tailwind 双 UI 体系并存；Vite 混合导入致拆包失效、chunk >500kB（BUG_SWEEP §15）。

**策略**：不搞一次性重写。规定**新页面/被改动页面必须用 react-query + 单一 UI 体系（定 AntD 6 为准）**，
配 ESLint 规则拦截手写 axios 与 Radix 新增引用；Vite 拆包问题单独修。

**验收标准**：
1. ESLint 规则上线：新增 `import axios` 于 pages/ 下直接报错（CI 拦截）。
2. P1-P3 涉及的页面（报价/搜索/通知/移动端）100% 走 react-query。
3. 首屏最大 chunk < 500kB，路由级拆包生效（构建产物断言脚本）。

---

## 明确不做（本期）

- **多语言 i18n**：无海外客户输入前不启动，全量硬编码中文的retrofit成本高、当前价值低（CHANGELOG 计划项，降级）。
- **知识图谱可视化**（backlog #21 后半）：先做 F4 RAG，图谱等检索被用起来再说。
- **主应用整体响应式改造**：只做 F9 车间移动路由，桌面管理页面不做移动适配。

## 与既有工作的衔接

- 测试债（全量 ~1,700 失败/490 错误，多为隔离债与漂移，见 BUG_SWEEP §三）**不阻塞本路线图**，
  但 F1/F2 新增测试必须用独立 fixture 写法，不再加重 `:memory:` 共享池冲突；隔离改造单独立项。
- FIX_SUMMARY 中"已修未提交"的缺陷（PR1/BOM1/OS1 等）先行提交合入，再开 F 系列分支。
- 每个特性完成定义（DoD）：验收标准全绿 + `pytest` 相关模块通过 + 真实浏览器过一遍主流程（沿用各链路验收报告的做法）。

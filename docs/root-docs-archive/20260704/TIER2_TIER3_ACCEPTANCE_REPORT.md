# 第二 / 第三梯队链路批量验收报告

**范围**：第二梯队(审批中心/PMO/预算/奖金/绩效合同/技术评审/项目复盘) + 第三梯队(角色/组织/HR/资质/人员匹配/战略/管理节奏/研发/知识库/供应商/任务中心)
**验收方式**：真实角色批量调各模块核心 create→action，聚焦"是否 500 崩溃 / 是否可用 / RBAC 与校验是否生效"，整库还原清理
**日期**：2026-07-02
**环境**：后端 `app.main:app`@127.0.0.1:8002；DB SQLite `data/app.db`（验收前整库备份，验收后已还原，零残留）

---

## 第二梯队（19/22，无 500 崩溃）

| 模块 | 结果 | 说明 |
|---|---|---|
| 预算 | ✅ 全流程 | 创建→提交→审批 全 2xx，DB 落库可核对 |
| 技术评审 | ✅ 创建 | 评审单创建 201，DB 落库可核对 |
| 奖金 | ⚠️ **仅冒烟** | 规则创建需 `hr:read` 权限(未建成)；项目奖金计算返回 **200 但 `data:[]`、message"未找到项目贡献记录"**——**接口未崩溃，但因无项目贡献数据未真正计算，计算逻辑未验证**（勘误：先前误记为"计算通过"） |
| PMO | ⚠️ | 风险(pmo副路径)/会议 422 字段校验；立项需 `project:initiation:create` |
| 通用审批中心 | ⚠️ | 有 RBAC(`approval:create`)；提交实例报『审批模板不存在』(**400 优雅**，非崩溃)——与 F1/ECN1/TS1 同源 |
| 绩效合同 | ⚠️ | 合约类型须 L1/L2/L3(校验生效) |
| 项目复盘 | ⚠️ | 422 字段校验 |

**第二梯队结论**：**无一个 500**。失败项均为权限门(RBAC 生效)或入参校验(422)，属正常防护。多数模块具 RBAC(approval/budget/task_center/pmo-initiation/hr/bonus)。审批模板缺失问题在通用审批中心以 400 优雅返回。

> ⚠️ **验证口径说明（重要）**：本批量验收对**计算/聚合类**端点（如奖金计算）仅做了"是否 500"冒烟，**200 不代表已正确计算**——奖金项目计算实际返回空(`data:[]`, "未找到项目贡献记录")，其计算逻辑**未被真正验证**（需先备齐项目贡献记录+适用规则再核对金额）。**创建类**端点(预算/技术评审等)有 DB 落库核对，结论可靠；**计算/聚合类**结论仅限"接口可达不崩溃"。

---

## 第三梯队（10/11 稳定，发现 1 个 500）

| 模块 | 结果 |
|---|---|
| 角色管理 | · 403 `role:create`(RBAC 生效) |
| 组织架构 | · 422 字段校验 |
| HR 合同 | · 404 员工不存在(校验生效) |
| 资质评估 | · 422 字段校验 |
| 人员匹配 | · 403 `staff_matching:create` |
| 战略年度工作 | · 422 字段校验 |
| 管理节奏 | · 422 字段校验 |
| 研发项目 | · 403 `rd_project:read` |
| 知识库 | · 403 `service:create` |
| 供应商 | · 403 `supplier:create` |
| **任务中心 建任务** | 🔴 **500 崩溃 → 已修复(TC1)** |

**第三梯队结论**：10/11 模块返回干净 4xx（RBAC 权限门或入参校验，均无崩溃，权限控制普遍到位）；仅任务中心建任务 500（TC1，已修复）。

---

## 发现问题（FINDINGS）

| 编号 | 严重度 | 模块 | 问题 | 状态 |
|---|---|---|---|---|
| **TC1** | 高 | 任务中心 | `task_center/create.py:107` 以位置参数调用带 `*,`(仅关键字)签名的路由函数 `get_task_detail(task.id, db, current_user)` → `TypeError: takes 0 positional arguments but 3 were given` → 500 | ✅ **已修复**（改为关键字调用，实测 201） |
| 审批模板缺失(复现) | 高 | 通用审批中心 | 提交审批实例报『审批模板不存在』——与 F1/ECN1/TS1 同源系统性问题 | 待修(任务#6) |

---

## 结论

第二、三梯队共 ~18 个支撑/后台模块**整体稳定**：仅发现 1 个新的代码级 500（任务中心 TC1，已修复），其余全部以 4xx（RBAC 权限门 / 入参校验）正常响应，未见崩溃。权限控制在第二三梯队普遍覆盖较好（多数模块有专属 `*:create/read/approve` 权限，与第一梯队生产/发货/售前"执行段无 RBAC"形成对比）。审批模板未初始化的系统性问题在通用审批中心再次复现（但以 400 优雅返回）。

**代码级 500 累计修复 6 个**：P1/PR1/AS1/BOM1/OS1（第一梯队）+ TC1（第三梯队）。

# 测试失败汇总（2026-04-14）

## 背景

本轮 `non-standard-automation-pms` 的覆盖率推进策略是：

- 优先补之前**未覆盖代码**
- 优先处理 **1 行 / 2 行 / 低依赖 / 纯 schema / compat / 导出层** 缺口
- 对全量测试跑出来的失败项，**先登记汇总，不在这一轮展开修复**

当前已知全项目 coverage 实测基线：

- `69.50%`
- 未覆盖 `48401` 行

> 说明：这个文件记录的是**失败问题的汇总视图**，不是逐条修复记录。

---

## 本轮失败项记录策略

- 失败项：**只登记汇总，不处理修复**
- 当前覆盖率推进期间，优先继续吃低风险缺口
- 高风险失败簇先挂起，后续单独开修复轮次

---

## 当前已知失败簇（按领域归类）

### 1. 权限 / 角色 / 报表框架
典型范围：
- 权限校验
- 角色与访问控制
- 报表框架相关依赖链

### 2. 绩效 / 工时 / 定时任务
典型范围：
- 绩效模块
- 工时记录、提醒、统计
- scheduler / 定时任务相关链路

### 3. 销售 / 售前 / 预测
典型范围：
- 销售域接口与 service
- 售前 AI 相关 schema / service
- 预测、报价、赢率、成本相关模块

### 4. 生产 / 采购 / 物料 / 库存
典型范围：
- 采购流程
- 物料、BOM、库存链路
- 生产计划与执行相关模块

### 5. 项目域
典型范围：
- 项目变更
- 项目风险
- 项目评审
- 项目成员、交付、成本相关链路

### 6. 安全 / 租户隔离 / API Key
典型范围：
- 多租户隔离
- 安全校验
- API Key / 认证相关逻辑

### 7. 成本预测 / 报表 / 通知
典型范围：
- 成本预测
- 报表导出/汇总
- 通知、消息、提醒相关 service

---

## 当前结论

1. 失败项已经有**分类汇总**，不是没记。
2. 但目前还是**按簇记录**，还不是逐条 testcase 清单。
3. 这一轮的主线仍是：
   - 先把低风险 coverage 缺口继续收掉
   - 失败簇后续单独开一轮清理

---

## 后续建议

如果下一步要把这里再做细，可以继续拆成两层：

1. **失败簇汇总**（保留本文件）
2. **逐条失败清单**（单独文件，记录具体测试名 / 报错 / 模块 / 优先级）

建议后续可新增：

- `docs/testing/test-failure-detail-2026-04-14.md`

---

## 更新规则

后续同类信息继续追加到这个文件，保持：

- 先写失败簇
- 再写是否进入修复
- 修完后在对应簇下回填结果

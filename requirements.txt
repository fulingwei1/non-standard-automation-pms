# Web框架
fastapi==0.115.0
uvicorn[standard]==0.32.0

# 数据库ORM
sqlalchemy==2.0.36

# 安全表达式评估 (用于预警规则引擎的条件表达式)
simpleeval==1.0.3  # 需要 1.0.3+ 支持 Python 3.12+

# 配置和验证
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.1.1  # Pydantic EmailStr 依赖

# 安全认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9  # API速率限制
pyotp==2.9.0  # TOTP双因素认证
qrcode[pil]==7.4.2  # 二维码生成

# 文件处理
python-multipart==0.0.12

# 数据处理
pandas==2.2.3
openpyxl==3.1.5

# 任务调度
apscheduler==3.10.4

# 文档处理
python-docx==1.1.2
PyPDF2==3.0.1
reportlab==4.2.5

# Redis支持
redis==5.2.0

# User-Agent解析
user-agents==2.2.0

# HTTP客户端（用于AI服务）
httpx==0.27.0

# Vercel Serverless 支持
mangum==0.17.0

# 测试
pytest>=8.2,<9
pytest-cov>=5.0
pytest-asyncio>=0.24
httpx==0.27.0  # 用于 API 测试
jinja2>=3.1.0

# 外协管理模块 详细设计文档

## 文档信息
- **模块名称**: 外协管理模块（Outsourcing Management）
- **版本**: v1.0
- **创建日期**: 2025-07-12
- **最后更新**: 2025-07-12
- **编写人**: System Architect
- **状态**: 初稿

---

## 1. 模块定位与目标

### 1.1 模块定位

外协管理模块是非标自动化项目管理系统的重要供应链协作模块，专门管理外协加工业务。非标自动化设备涉及大量机加工件、钣金件、表面处理等工序，这些工序通常需要外发给专业供应商完成。本模块实现从外协需求发起、供应商选择、订单跟踪到来料检验的全流程闭环管理。

### 1.2 外协业务特点

```
非标自动化外协业务特点：

┌─────────────────────────────────────────────────────────────────┐
│                        外协业务特点                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 小批量多品种                                                │
│     └─ 每个项目零件不同，批量小（1-10件为主）                   │
│                                                                 │
│  2. 交期紧张                                                    │
│     └─ 项目周期短，外协件是关键路径                             │
│                                                                 │
│  3. 精度要求高                                                  │
│     └─ 非标设备对加工精度要求严格                               │
│                                                                 │
│  4. 供应商分散                                                  │
│     └─ 不同工序需要不同专业供应商                               │
│                                                                 │
│  5. 图纸频繁变更                                                │
│     └─ 设计变更导致外协件需要重新加工                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 核心目标

| 目标 | 描述 |
|------|------|
| 外协全流程管理 | 从需求到验收的完整外协业务流程 |
| 供应商协同 | 供应商门户、进度反馈、文件传递 |
| 交期监控 | 实时跟踪外协进度，预警交期风险 |
| 质量追溯 | 来料检验记录，质量问题追溯 |
| 成本管控 | 外协成本统计分析，控制加工费用 |

### 1.4 外协类型定义

| 外协类型 | 代码 | 典型工序 | 周期特点 |
|----------|------|----------|----------|
| 机加工 | MACHINING | CNC加工、车削、铣削、磨削 | 3-7天 |
| 钣金加工 | SHEET_METAL | 激光切割、折弯、焊接 | 2-5天 |
| 表面处理 | SURFACE | 阳极氧化、电镀、喷涂、发黑 | 3-5天 |
| 热处理 | HEAT_TREAT | 淬火、回火、渗碳、氮化 | 2-4天 |
| 线切割 | WIRE_CUT | 慢走丝、快走丝、电火花 | 2-5天 |
| 精密研磨 | GRINDING | 平面磨、外圆磨、内圆磨 | 1-3天 |
| 装配外协 | ASSEMBLY | 组件装配、调试 | 5-15天 |

---

## 2. 业务流程设计

### 2.1 外协业务全流程

```
外协业务全流程：

┌──────────────────────────────────────────────────────────────────┐
│                        外协业务流程                               │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. 需求发起                                                      │
│     ├── 设计出图，确定外协件清单                                  │
│     ├── 工程师提交外协需求                                        │
│     └── 关联项目/BOM/图纸                                         │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  2. 询比价                                                        │
│     ├── 选择候选供应商（2-3家）                                   │
│     ├── 发送询价单（含图纸）                                      │
│     ├── 供应商报价                                                │
│     └── 比价分析                                                  │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  3. 下单发料                                                      │
│     ├── 选定供应商，生成外协订单                                  │
│     ├── 采购审批（金额超限时）                                    │
│     ├── 发送订单给供应商                                          │
│     └── 安排发料（如需提供原材料）                                │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  4. 生产跟踪                                                      │
│     ├── 供应商确认接单                                            │
│     ├── 供应商更新生产进度                                        │
│     ├── 采购跟催（接近交期时）                                    │
│     └── 异常处理（延期/质量问题）                                 │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  5. 到货检验                                                      │
│     ├── 供应商发货                                                │
│     ├── 仓库收货登记                                              │
│     ├── 质量来料检验（IQC）                                       │
│     └── 入库/退货处理                                             │
│                                                                   │
│             │                                                     │
│             ▼                                                     │
│                                                                   │
│  6. 结算对账                                                      │
│     ├── 生成对账单                                                │
│     ├── 供应商确认                                                │
│     ├── 发票登记                                                  │
│     └── 付款申请                                                  │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 外协订单状态流转

```
外协订单状态流转图：

  ┌─────────┐
  │  DRAFT  │ 草稿（创建外协单）
  └────┬────┘
       │ 提交
       ▼
  ┌──────────┐
  │ PENDING  │ 待审批（金额超限）
  │ APPROVAL │
  └────┬─────┘
       │ 审批通过
       ▼
  ┌──────────┐
  │ CONFIRMED│ 已确认（待发送供应商）
  └────┬─────┘
       │ 发送订单
       ▼
  ┌──────────┐
  │  SENT    │ 已发送（等待供应商确认）
  └────┬─────┘
       │ 供应商确认
       ▼
  ┌──────────────┐
  │ IN_PRODUCTION│ 生产中
  └──────┬───────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌──────────┐
│SHIPPED │ │ DELAYED  │ 已延期
└───┬────┘ └────┬─────┘
    │           │ 延期处理后发货
    └─────┬─────┘
          ▼
    ┌──────────┐
    │ RECEIVED │ 已收货（待检验）
    └────┬─────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌──────────┐
│ PASSED │ │ REJECTED │ 检验不合格
└───┬────┘ └────┬─────┘
    │           │ 返工/退货
    │           ▼
    │      ┌──────────┐
    │      │ REWORKING│ 返工中
    │      └────┬─────┘
    │           │
    └─────┬─────┘
          ▼
    ┌───────────┐
    │ COMPLETED │ 已完成
    └───────────┘
```

### 2.3 来料检验流程

```
来料检验（IQC）流程：

收货登记
    │
    ▼
┌──────────────┐
│ 创建检验单   │ 自动关联外协订单
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ 首件检验     │ 核心尺寸、外观
└──────┬───────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
 合格    不合格
   │       │
   ▼       ▼
┌──────┐ ┌──────────┐
│抽检  │ │全检/退货 │
└──┬───┘ └────┬─────┘
   │          │
   ▼          │
┌───────────┐ │
│ 抽检结果  │ │
└─────┬─────┘ │
      │       │
  ┌───┴───┐   │
  │       │   │
  ▼       ▼   │
合格    不合格 │
  │       │   │
  │   ┌───┴───┘
  │   │
  ▼   ▼
┌────┐ ┌──────────┐
│入库│ │退货/返工 │
└────┘ └──────────┘
```

---

## 3. 数据模型设计

### 3.1 核心实体关系

```
┌─────────────────────────────────────────────────────────────────┐
│                        实体关系图                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐     ┌────────────────┐                       │
│  │ outsourcing_ │────→│ outsourcing_   │                       │
│  │ suppliers    │     │ orders         │                       │
│  └──────────────┘     └───────┬────────┘                       │
│                               │                                 │
│  ┌──────────────┐             │                                 │
│  │ projects     │─────────────┤                                 │
│  └──────────────┘             │                                 │
│                               ▼                                 │
│  ┌──────────────┐     ┌────────────────┐                       │
│  │ materials    │────→│ outsourcing_   │                       │
│  │ (BOM)        │     │ order_items    │                       │
│  └──────────────┘     └───────┬────────┘                       │
│                               │                                 │
│                               │                                 │
│  ┌────────────────────────────┼───────────────────────────┐    │
│  │                            │                           │    │
│  ▼                            ▼                           ▼    │
│  ┌──────────────┐     ┌────────────────┐     ┌──────────────┐ │
│  │ outsourcing_ │     │ iqc_inspections│     │ outsourcing_ │ │
│  │ progress     │     │                │     │ settlements  │ │
│  └──────────────┘     └────────────────┘     └──────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 数据表设计

#### 3.2.1 外协供应商表（outsourcing_suppliers）

```sql
CREATE TABLE outsourcing_suppliers (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    supplier_code       VARCHAR(50) NOT NULL UNIQUE,      -- 供应商编码
    supplier_name       VARCHAR(200) NOT NULL,            -- 供应商名称
    short_name          VARCHAR(50),                      -- 简称
    supplier_type       VARCHAR(20) NOT NULL,             -- 类型：MACHINING/SHEET_METAL等

    -- 联系信息
    contact_person      VARCHAR(50),                      -- 联系人
    contact_phone       VARCHAR(50),                      -- 联系电话
    contact_email       VARCHAR(100),                     -- 邮箱
    address             VARCHAR(500),                     -- 地址

    -- 能力信息
    capabilities        TEXT,                             -- 加工能力描述JSON
    equipment_list      TEXT,                             -- 设备清单JSON
    certification       TEXT,                             -- 资质证书JSON
    min_order_amount    DECIMAL(12,2) DEFAULT 0,          -- 最小订单金额
    lead_time_days      INTEGER DEFAULT 7,                -- 标准交期（天）

    -- 评级信息
    quality_rating      DECIMAL(3,2) DEFAULT 0,           -- 质量评分(0-5)
    delivery_rating     DECIMAL(3,2) DEFAULT 0,           -- 交期评分(0-5)
    service_rating      DECIMAL(3,2) DEFAULT 0,           -- 服务评分(0-5)
    overall_rating      DECIMAL(3,2) DEFAULT 0,           -- 综合评分(0-5)
    rating_count        INTEGER DEFAULT 0,                -- 评价次数

    -- 合作信息
    cooperation_status  VARCHAR(20) DEFAULT 'ACTIVE',     -- ACTIVE/SUSPENDED/BLACKLIST
    contract_start_date DATE,                             -- 合同开始日期
    contract_end_date   DATE,                             -- 合同结束日期
    payment_terms       VARCHAR(50),                      -- 付款条款
    credit_limit        DECIMAL(12,2),                    -- 信用额度

    -- 门户账号
    portal_enabled      BOOLEAN DEFAULT 0,                -- 是否启用门户
    portal_username     VARCHAR(100),                     -- 门户用户名
    portal_password_hash VARCHAR(256),                    -- 门户密码哈希

    -- 统计
    total_orders        INTEGER DEFAULT 0,                -- 历史订单数
    total_amount        DECIMAL(14,2) DEFAULT 0,          -- 历史订单金额

    created_by          INTEGER,
    created_at          DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at          DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE INDEX idx_outsourcing_suppliers_code ON outsourcing_suppliers(supplier_code);
CREATE INDEX idx_outsourcing_suppliers_type ON outsourcing_suppliers(supplier_type);
CREATE INDEX idx_outsourcing_suppliers_status ON outsourcing_suppliers(cooperation_status);
```

#### 3.2.2 供应商能力矩阵表（supplier_capabilities）

```sql
CREATE TABLE supplier_capabilities (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    supplier_id     INTEGER NOT NULL,                     -- 供应商ID
    process_type    VARCHAR(50) NOT NULL,                 -- 工序类型
    process_name    VARCHAR(100) NOT NULL,                -- 工序名称

    -- 能力参数
    max_size_x      DECIMAL(10,2),                        -- 最大尺寸X(mm)
    max_size_y      DECIMAL(10,2),                        -- 最大尺寸Y(mm)
    max_size_z      DECIMAL(10,2),                        -- 最大尺寸Z(mm)
    max_weight      DECIMAL(10,2),                        -- 最大重量(kg)
    precision_level VARCHAR(20),                          -- 精度等级
    tolerance_min   DECIMAL(10,4),                        -- 最小公差(mm)

    -- 价格信息
    base_price      DECIMAL(10,2),                        -- 基础单价
    price_unit      VARCHAR(20),                          -- 计价单位（件/小时/公斤）

    -- 交期
    standard_days   INTEGER,                              -- 标准交期（天）
    urgent_days     INTEGER,                              -- 加急交期（天）
    urgent_ratio    DECIMAL(5,2) DEFAULT 1.5,             -- 加急费率

    remark          TEXT,                                 -- 备注

    FOREIGN KEY (supplier_id) REFERENCES outsourcing_suppliers(id)
);

CREATE INDEX idx_supplier_capabilities_supplier ON supplier_capabilities(supplier_id);
CREATE INDEX idx_supplier_capabilities_process ON supplier_capabilities(process_type);
```

#### 3.2.3 外协订单表（outsourcing_orders）

```sql
CREATE TABLE outsourcing_orders (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    order_no            VARCHAR(50) NOT NULL UNIQUE,      -- 外协单号
    project_id          INTEGER NOT NULL,                 -- 项目ID
    supplier_id         INTEGER NOT NULL,                 -- 供应商ID
    process_type        VARCHAR(50) NOT NULL,             -- 工序类型

    -- 订单信息
    order_type          VARCHAR(20) DEFAULT 'NORMAL',     -- NORMAL/URGENT/REWORK
    priority            VARCHAR(20) DEFAULT 'NORMAL',     -- LOW/NORMAL/HIGH/URGENT

    -- 时间
    order_date          DATE NOT NULL,                    -- 下单日期
    required_date       DATE NOT NULL,                    -- 要求交期
    confirmed_date      DATE,                             -- 供应商确认交期
    actual_ship_date    DATE,                             -- 实际发货日期
    actual_receive_date DATE,                             -- 实际收货日期

    -- 金额
    total_quantity      INTEGER DEFAULT 0,                -- 总数量
    total_amount        DECIMAL(12,2) DEFAULT 0,          -- 总金额
    tax_rate            DECIMAL(5,2) DEFAULT 13,          -- 税率
    tax_amount          DECIMAL(12,2) DEFAULT 0,          -- 税额
    grand_total         DECIMAL(12,2) DEFAULT 0,          -- 价税合计

    -- 发料信息
    material_provided   BOOLEAN DEFAULT 0,                -- 是否提供原材料
    material_issue_date DATE,                             -- 发料日期
    material_issue_note VARCHAR(500),                     -- 发料备注

    -- 状态
    status              VARCHAR(20) DEFAULT 'DRAFT',      -- 订单状态
    approval_status     VARCHAR(20),                      -- 审批状态

    -- 检验结果
    iqc_status          VARCHAR(20),                      -- IQC状态
    iqc_passed_qty      INTEGER DEFAULT 0,                -- 检验合格数量
    iqc_rejected_qty    INTEGER DEFAULT 0,                -- 检验不合格数量

    -- 结算
    settlement_status   VARCHAR(20) DEFAULT 'UNSETTLED',  -- 结算状态
    invoice_no          VARCHAR(100),                     -- 发票号
    invoice_date        DATE,                             -- 发票日期
    payment_status      VARCHAR(20) DEFAULT 'UNPAID',     -- 付款状态

    -- 备注
    remark              TEXT,                             -- 订单备注
    internal_note       TEXT,                             -- 内部备注（不发给供应商）

    -- 附件
    attachments         TEXT,                             -- 附件列表JSON

    created_by          INTEGER,
    created_at          DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at          DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (project_id) REFERENCES projects(id),
    FOREIGN KEY (supplier_id) REFERENCES outsourcing_suppliers(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE INDEX idx_outsourcing_orders_no ON outsourcing_orders(order_no);
CREATE INDEX idx_outsourcing_orders_project ON outsourcing_orders(project_id);
CREATE INDEX idx_outsourcing_orders_supplier ON outsourcing_orders(supplier_id);
CREATE INDEX idx_outsourcing_orders_status ON outsourcing_orders(status);
CREATE INDEX idx_outsourcing_orders_date ON outsourcing_orders(order_date);
```

#### 3.2.4 外协订单明细表（outsourcing_order_items）

```sql
CREATE TABLE outsourcing_order_items (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    order_id            INTEGER NOT NULL,                 -- 外协单ID
    item_no             INTEGER NOT NULL,                 -- 行号

    -- 物料信息
    material_id         INTEGER,                          -- 物料ID（关联BOM）
    part_code           VARCHAR(100) NOT NULL,            -- 零件编码
    part_name           VARCHAR(200) NOT NULL,            -- 零件名称
    drawing_no          VARCHAR(100),                     -- 图号
    drawing_version     VARCHAR(20),                      -- 图纸版本
    specification       VARCHAR(500),                     -- 规格描述
    material            VARCHAR(100),                     -- 材质

    -- 工序信息
    process_type        VARCHAR(50),                      -- 工序类型
    process_desc        TEXT,                             -- 加工要求描述
    surface_treatment   VARCHAR(100),                     -- 表面处理要求

    -- 数量与价格
    quantity            INTEGER NOT NULL,                 -- 数量
    unit                VARCHAR(20) DEFAULT '件',         -- 单位
    unit_price          DECIMAL(10,2) NOT NULL,           -- 单价
    amount              DECIMAL(12,2) NOT NULL,           -- 金额

    -- 交期
    required_date       DATE,                             -- 要求交期（可单独设置）

    -- 收货信息
    received_qty        INTEGER DEFAULT 0,                -- 已收数量
    passed_qty          INTEGER DEFAULT 0,                -- 合格数量
    rejected_qty        INTEGER DEFAULT 0,                -- 不合格数量

    -- 状态
    status              VARCHAR(20) DEFAULT 'PENDING',    -- 行状态

    -- 图纸附件
    drawing_files       TEXT,                             -- 图纸文件JSON

    remark              TEXT,                             -- 备注

    FOREIGN KEY (order_id) REFERENCES outsourcing_orders(id),
    FOREIGN KEY (material_id) REFERENCES materials(id)
);

CREATE INDEX idx_outsourcing_order_items_order ON outsourcing_order_items(order_id);
CREATE INDEX idx_outsourcing_order_items_material ON outsourcing_order_items(material_id);
```

#### 3.2.5 外协进度跟踪表（outsourcing_progress）

```sql
CREATE TABLE outsourcing_progress (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    order_id        INTEGER NOT NULL,                     -- 外协单ID
    order_item_id   INTEGER,                              -- 外协单明细ID（可选）

    -- 进度信息
    progress_date   DATE NOT NULL,                        -- 进度日期
    progress_type   VARCHAR(20) NOT NULL,                 -- PRODUCTION/QUALITY/SHIPPING
    progress_status VARCHAR(50) NOT NULL,                 -- 进度状态
    progress_pct    INTEGER DEFAULT 0,                    -- 完成百分比
    completed_qty   INTEGER DEFAULT 0,                    -- 已完成数量

    -- 描述
    description     TEXT,                                 -- 进度描述
    issue_desc      TEXT,                                 -- 问题描述

    -- 预计信息
    estimated_ship_date DATE,                             -- 预计发货日期

    -- 附件（进度图片等）
    attachments     TEXT,                                 -- 附件JSON

    -- 来源
    source          VARCHAR(20) DEFAULT 'INTERNAL',       -- INTERNAL/SUPPLIER/SYSTEM
    reported_by     INTEGER,                              -- 填报人
    reported_at     DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (order_id) REFERENCES outsourcing_orders(id),
    FOREIGN KEY (order_item_id) REFERENCES outsourcing_order_items(id),
    FOREIGN KEY (reported_by) REFERENCES users(id)
);

CREATE INDEX idx_outsourcing_progress_order ON outsourcing_progress(order_id);
CREATE INDEX idx_outsourcing_progress_date ON outsourcing_progress(progress_date);
```

#### 3.2.6 来料检验表（iqc_inspections）

```sql
CREATE TABLE iqc_inspections (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    inspection_no       VARCHAR(50) NOT NULL UNIQUE,      -- 检验单号
    order_id            INTEGER NOT NULL,                 -- 外协单ID
    order_item_id       INTEGER,                          -- 外协单明细ID

    -- 收货信息
    receive_date        DATE NOT NULL,                    -- 收货日期
    receive_qty         INTEGER NOT NULL,                 -- 收货数量
    batch_no            VARCHAR(50),                      -- 批次号

    -- 检验信息
    inspection_type     VARCHAR(20) DEFAULT 'SAMPLING',   -- FULL/SAMPLING/FIRST
    sample_qty          INTEGER,                          -- 抽样数量
    inspection_date     DATETIME,                         -- 检验日期
    inspector_id        INTEGER,                          -- 检验员

    -- 检验结果
    result              VARCHAR(20),                      -- PASSED/REJECTED/CONDITIONAL
    passed_qty          INTEGER DEFAULT 0,                -- 合格数量
    rejected_qty        INTEGER DEFAULT 0,                -- 不合格数量

    -- 不合格原因
    defect_type         VARCHAR(50),                      -- 缺陷类型
    defect_desc         TEXT,                             -- 缺陷描述

    -- 处理方式
    disposition         VARCHAR(20),                      -- ACCEPT/REJECT/REWORK/CONCESSION
    disposition_note    TEXT,                             -- 处理说明

    -- 入库信息
    warehouse_in_qty    INTEGER DEFAULT 0,                -- 入库数量
    warehouse_in_date   DATETIME,                         -- 入库日期

    -- 附件
    inspection_report   TEXT,                             -- 检验报告文件
    defect_photos       TEXT,                             -- 缺陷照片JSON

    created_by          INTEGER,
    created_at          DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at          DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (order_id) REFERENCES outsourcing_orders(id),
    FOREIGN KEY (order_item_id) REFERENCES outsourcing_order_items(id),
    FOREIGN KEY (inspector_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE INDEX idx_iqc_inspections_order ON iqc_inspections(order_id);
CREATE INDEX idx_iqc_inspections_date ON iqc_inspections(receive_date);
CREATE INDEX idx_iqc_inspections_result ON iqc_inspections(result);
```

#### 3.2.7 检验明细表（iqc_inspection_items）

```sql
CREATE TABLE iqc_inspection_items (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    inspection_id       INTEGER NOT NULL,                 -- 检验单ID
    item_no             INTEGER NOT NULL,                 -- 项号

    -- 检验项目
    check_item          VARCHAR(200) NOT NULL,            -- 检验项目
    check_method        VARCHAR(200),                     -- 检验方法
    standard_value      VARCHAR(100),                     -- 标准值
    tolerance_min       VARCHAR(50),                      -- 下限
    tolerance_max       VARCHAR(50),                      -- 上限
    unit                VARCHAR(20),                      -- 单位

    -- 检验结果
    actual_value        VARCHAR(100),                     -- 实际值
    result              VARCHAR(20),                      -- PASSED/FAILED

    remark              TEXT,                             -- 备注

    FOREIGN KEY (inspection_id) REFERENCES iqc_inspections(id)
);

CREATE INDEX idx_iqc_inspection_items_inspection ON iqc_inspection_items(inspection_id);
```

#### 3.2.8 外协结算表（outsourcing_settlements）

```sql
CREATE TABLE outsourcing_settlements (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    settlement_no       VARCHAR(50) NOT NULL UNIQUE,      -- 结算单号
    supplier_id         INTEGER NOT NULL,                 -- 供应商ID

    -- 结算周期
    period_start        DATE NOT NULL,                    -- 结算开始日期
    period_end          DATE NOT NULL,                    -- 结算结束日期

    -- 金额
    order_count         INTEGER DEFAULT 0,                -- 订单数量
    total_amount        DECIMAL(14,2) DEFAULT 0,          -- 总金额（不含税）
    tax_amount          DECIMAL(12,2) DEFAULT 0,          -- 税额
    grand_total         DECIMAL(14,2) DEFAULT 0,          -- 价税合计

    -- 扣款
    deduction_amount    DECIMAL(12,2) DEFAULT 0,          -- 扣款金额
    deduction_reason    TEXT,                             -- 扣款原因

    -- 实付
    payable_amount      DECIMAL(14,2) DEFAULT 0,          -- 应付金额

    -- 状态
    status              VARCHAR(20) DEFAULT 'DRAFT',      -- DRAFT/PENDING/CONFIRMED/INVOICED/PAID

    -- 供应商确认
    supplier_confirmed  BOOLEAN DEFAULT 0,                -- 供应商已确认
    supplier_confirmed_at DATETIME,                       -- 确认时间
    supplier_confirmed_by VARCHAR(100),                   -- 确认人

    -- 发票信息
    invoice_no          VARCHAR(100),                     -- 发票号
    invoice_date        DATE,                             -- 发票日期
    invoice_amount      DECIMAL(14,2),                    -- 发票金额

    -- 付款信息
    payment_date        DATE,                             -- 付款日期
    payment_amount      DECIMAL(14,2),                    -- 付款金额
    payment_voucher     VARCHAR(100),                     -- 付款凭证号

    remark              TEXT,                             -- 备注

    created_by          INTEGER,
    created_at          DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at          DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (supplier_id) REFERENCES outsourcing_suppliers(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);

CREATE INDEX idx_outsourcing_settlements_supplier ON outsourcing_settlements(supplier_id);
CREATE INDEX idx_outsourcing_settlements_period ON outsourcing_settlements(period_start, period_end);
CREATE INDEX idx_outsourcing_settlements_status ON outsourcing_settlements(status);
```

#### 3.2.9 结算明细表（settlement_order_items）

```sql
CREATE TABLE settlement_order_items (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    settlement_id   INTEGER NOT NULL,                     -- 结算单ID
    order_id        INTEGER NOT NULL,                     -- 外协单ID

    -- 金额
    order_amount    DECIMAL(12,2) NOT NULL,               -- 订单金额
    deduction       DECIMAL(12,2) DEFAULT 0,              -- 扣款
    settle_amount   DECIMAL(12,2) NOT NULL,               -- 结算金额

    FOREIGN KEY (settlement_id) REFERENCES outsourcing_settlements(id),
    FOREIGN KEY (order_id) REFERENCES outsourcing_orders(id)
);

CREATE INDEX idx_settlement_order_items_settlement ON settlement_order_items(settlement_id);
```

#### 3.2.10 供应商评价表（supplier_evaluations）

```sql
CREATE TABLE supplier_evaluations (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    supplier_id         INTEGER NOT NULL,                 -- 供应商ID
    order_id            INTEGER,                          -- 关联订单ID
    evaluation_type     VARCHAR(20) NOT NULL,             -- ORDER/MONTHLY/ANNUAL
    evaluation_period   VARCHAR(20),                      -- 评价周期（如2025-01）

    -- 评分
    quality_score       DECIMAL(3,2),                     -- 质量评分(0-5)
    delivery_score      DECIMAL(3,2),                     -- 交期评分(0-5)
    service_score       DECIMAL(3,2),                     -- 服务评分(0-5)
    price_score         DECIMAL(3,2),                     -- 价格评分(0-5)
    overall_score       DECIMAL(3,2),                     -- 综合评分(0-5)

    -- 详细评价
    quality_comment     TEXT,                             -- 质量评价
    delivery_comment    TEXT,                             -- 交期评价
    service_comment     TEXT,                             -- 服务评价
    overall_comment     TEXT,                             -- 综合评价

    -- 改进建议
    improvement_suggest TEXT,                             -- 改进建议

    evaluator_id        INTEGER,                          -- 评价人
    evaluated_at        DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (supplier_id) REFERENCES outsourcing_suppliers(id),
    FOREIGN KEY (order_id) REFERENCES outsourcing_orders(id),
    FOREIGN KEY (evaluator_id) REFERENCES users(id)
);

CREATE INDEX idx_supplier_evaluations_supplier ON supplier_evaluations(supplier_id);
CREATE INDEX idx_supplier_evaluations_period ON supplier_evaluations(evaluation_period);
```

---

## 4. API设计

### 4.1 外协供应商API

```yaml
# 供应商管理API
/api/v1/outsourcing/suppliers:
  GET:
    summary: 获取供应商列表
    parameters:
      - supplier_type: string      # 供应商类型
      - cooperation_status: string # 合作状态
      - keyword: string            # 搜索关键词
      - page: integer
      - page_size: integer
    responses:
      200:
        items: Supplier[]
        total: integer

  POST:
    summary: 创建供应商
    requestBody:
      supplier_code: string
      supplier_name: string
      supplier_type: string
      contact_person: string
      contact_phone: string
      capabilities: object[]
    responses:
      201:
        supplier_id: integer

/api/v1/outsourcing/suppliers/{id}:
  GET:
    summary: 获取供应商详情
  PUT:
    summary: 更新供应商
  DELETE:
    summary: 删除供应商（软删除）

/api/v1/outsourcing/suppliers/{id}/capabilities:
  GET:
    summary: 获取供应商能力矩阵
  PUT:
    summary: 更新供应商能力

/api/v1/outsourcing/suppliers/{id}/evaluations:
  GET:
    summary: 获取供应商评价历史
  POST:
    summary: 添加供应商评价

/api/v1/outsourcing/suppliers/{id}/orders:
  GET:
    summary: 获取供应商历史订单

/api/v1/outsourcing/suppliers/recommend:
  POST:
    summary: 推荐供应商
    requestBody:
      process_type: string
      max_size: object
      required_date: date
    responses:
      200:
        suppliers: RecommendedSupplier[]
```

### 4.2 外协订单API

```yaml
# 外协订单API
/api/v1/outsourcing/orders:
  GET:
    summary: 获取外协订单列表
    parameters:
      - project_id: integer
      - supplier_id: integer
      - status: string
      - process_type: string
      - date_from: date
      - date_to: date
    responses:
      200:
        items: OutsourcingOrder[]

  POST:
    summary: 创建外协订单
    requestBody:
      project_id: integer
      supplier_id: integer
      process_type: string
      required_date: date
      items:
        - part_code: string
          part_name: string
          drawing_no: string
          quantity: integer
          unit_price: number
    responses:
      201:
        order_id: integer
        order_no: string

/api/v1/outsourcing/orders/{id}:
  GET:
    summary: 获取订单详情
  PUT:
    summary: 更新订单
  DELETE:
    summary: 删除订单（仅草稿）

/api/v1/outsourcing/orders/{id}/submit:
  POST:
    summary: 提交订单审批

/api/v1/outsourcing/orders/{id}/approve:
  POST:
    summary: 审批订单
    requestBody:
      approved: boolean
      comment: string

/api/v1/outsourcing/orders/{id}/send:
  POST:
    summary: 发送订单给供应商

/api/v1/outsourcing/orders/{id}/confirm:
  POST:
    summary: 供应商确认订单
    requestBody:
      confirmed_date: date
      remark: string

/api/v1/outsourcing/orders/{id}/progress:
  GET:
    summary: 获取订单进度
  POST:
    summary: 更新订单进度
    requestBody:
      progress_status: string
      progress_pct: integer
      completed_qty: integer
      description: string
      estimated_ship_date: date

/api/v1/outsourcing/orders/{id}/ship:
  POST:
    summary: 供应商发货
    requestBody:
      ship_date: date
      tracking_no: string
      ship_qty: integer

/api/v1/outsourcing/orders/{id}/receive:
  POST:
    summary: 收货登记
    requestBody:
      receive_date: date
      receive_qty: integer
      batch_no: string
```

### 4.3 来料检验API

```yaml
# 来料检验API
/api/v1/outsourcing/iqc:
  GET:
    summary: 获取检验单列表
    parameters:
      - order_id: integer
      - result: string
      - date_from: date
      - date_to: date
    responses:
      200:
        items: IQCInspection[]

  POST:
    summary: 创建检验单
    requestBody:
      order_id: integer
      order_item_id: integer
      receive_qty: integer
      batch_no: string
      inspection_type: string
    responses:
      201:
        inspection_id: integer
        inspection_no: string

/api/v1/outsourcing/iqc/{id}:
  GET:
    summary: 获取检验单详情
  PUT:
    summary: 更新检验单

/api/v1/outsourcing/iqc/{id}/items:
  GET:
    summary: 获取检验项目
  PUT:
    summary: 批量更新检验结果
    requestBody:
      items:
        - item_no: integer
          actual_value: string
          result: string

/api/v1/outsourcing/iqc/{id}/complete:
  POST:
    summary: 完成检验
    requestBody:
      result: string          # PASSED/REJECTED/CONDITIONAL
      passed_qty: integer
      rejected_qty: integer
      disposition: string
      disposition_note: string

/api/v1/outsourcing/iqc/{id}/warehouse-in:
  POST:
    summary: 合格品入库
    requestBody:
      warehouse_in_qty: integer
```

### 4.4 结算API

```yaml
# 结算API
/api/v1/outsourcing/settlements:
  GET:
    summary: 获取结算单列表
    parameters:
      - supplier_id: integer
      - status: string
      - period: string
    responses:
      200:
        items: Settlement[]

  POST:
    summary: 生成结算单
    requestBody:
      supplier_id: integer
      period_start: date
      period_end: date
    responses:
      201:
        settlement_id: integer
        settlement_no: string

/api/v1/outsourcing/settlements/{id}:
  GET:
    summary: 获取结算单详情
  PUT:
    summary: 更新结算单

/api/v1/outsourcing/settlements/{id}/confirm:
  POST:
    summary: 确认结算单

/api/v1/outsourcing/settlements/{id}/invoice:
  POST:
    summary: 登记发票
    requestBody:
      invoice_no: string
      invoice_date: date
      invoice_amount: number

/api/v1/outsourcing/settlements/{id}/payment:
  POST:
    summary: 登记付款
    requestBody:
      payment_date: date
      payment_amount: number
      payment_voucher: string
```

### 4.5 供应商门户API

```yaml
# 供应商门户API
/api/v1/portal/supplier/login:
  POST:
    summary: 供应商登录
    requestBody:
      username: string
      password: string
    responses:
      200:
        token: string
        supplier: SupplierInfo

/api/v1/portal/supplier/orders:
  GET:
    summary: 获取待处理订单
    headers:
      X-Supplier-Token: string
    responses:
      200:
        items: Order[]

/api/v1/portal/supplier/orders/{id}:
  GET:
    summary: 获取订单详情
    headers:
      X-Supplier-Token: string

/api/v1/portal/supplier/orders/{id}/confirm:
  POST:
    summary: 确认订单
    headers:
      X-Supplier-Token: string
    requestBody:
      confirmed_date: date
      remark: string

/api/v1/portal/supplier/orders/{id}/progress:
  POST:
    summary: 更新进度
    headers:
      X-Supplier-Token: string
    requestBody:
      progress_status: string
      completed_qty: integer
      estimated_ship_date: date
      description: string
      attachments: string[]

/api/v1/portal/supplier/orders/{id}/ship:
  POST:
    summary: 发货通知
    headers:
      X-Supplier-Token: string
    requestBody:
      ship_date: date
      ship_qty: integer
      tracking_no: string

/api/v1/portal/supplier/settlements:
  GET:
    summary: 获取对账单
    headers:
      X-Supplier-Token: string

/api/v1/portal/supplier/settlements/{id}/confirm:
  POST:
    summary: 确认对账单
    headers:
      X-Supplier-Token: string
```

---

## 5. 业务规则

### 5.1 编号规则

```
外协订单编号：OC-{年份后两位}{月份}-{4位流水号}
示例：OC-2501-0001

检验单编号：IQC-{年份后两位}{月份}-{4位流水号}
示例：IQC-2501-0001

结算单编号：ST-{供应商编码}-{年月}
示例：ST-SUP001-202501
```

### 5.2 审批规则

| 条件 | 审批流程 |
|------|----------|
| 金额 ≤ 5000元 | 无需审批，直接确认 |
| 5000 < 金额 ≤ 20000元 | 采购主管审批 |
| 金额 > 20000元 | 采购主管 → 财务审批 |
| 加急订单 | 需额外说明加急原因 |

### 5.3 交期预警规则

| 预警级别 | 条件 | 触发动作 |
|----------|------|----------|
| 黄色预警 | 距交期≤3天，进度<80% | 系统通知采购员 |
| 橙色预警 | 距交期≤1天，进度<90% | 通知采购主管 |
| 红色预警 | 已超期 | 通知项目经理、采购主管 |

### 5.4 检验规则

```python
def determine_inspection_type(order_item, supplier):
    """
    确定检验方式
    """
    # 首次合作供应商：全检
    if supplier.total_orders == 0:
        return 'FULL'

    # 供应商质量评分低于3.5：全检
    if supplier.quality_rating < 3.5:
        return 'FULL'

    # 关键零件：全检
    if order_item.is_critical:
        return 'FULL'

    # 其他情况：抽检
    return 'SAMPLING'


def calculate_sample_qty(total_qty, aql_level='II'):
    """
    计算抽样数量（简化版GB/T 2828.1）
    """
    sample_table = {
        (2, 8): 2,
        (9, 15): 3,
        (16, 25): 5,
        (26, 50): 8,
        (51, 90): 13,
        (91, 150): 20,
        (151, 280): 32,
        (281, 500): 50,
        (501, 1200): 80,
    }

    for (min_qty, max_qty), sample_size in sample_table.items():
        if min_qty <= total_qty <= max_qty:
            return sample_size

    return min(125, total_qty)
```

### 5.5 供应商评分规则

```python
def calculate_supplier_rating(order):
    """
    单次订单评分计算
    """
    # 交期评分
    if order.actual_receive_date <= order.required_date:
        delivery_score = 5.0
    else:
        delay_days = (order.actual_receive_date - order.required_date).days
        delivery_score = max(0, 5.0 - delay_days * 0.5)

    # 质量评分
    if order.iqc_passed_qty == order.total_quantity:
        quality_score = 5.0
    else:
        pass_rate = order.iqc_passed_qty / order.total_quantity
        quality_score = pass_rate * 5.0

    # 综合评分（质量权重60%，交期权重40%）
    overall_score = quality_score * 0.6 + delivery_score * 0.4

    return {
        'quality_score': quality_score,
        'delivery_score': delivery_score,
        'overall_score': overall_score
    }
```

---

## 6. UI设计

### 6.1 外协订单列表

```
┌─────────────────────────────────────────────────────────────────┐
│  外协管理                                        [+ 新建外协单]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ [全部] [待审批] [待发送] [生产中] [已发货] [待检验]       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 🔍 外协单号/项目 [供应商▼] [工序类型▼] [日期范围] [搜索] │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 外协单号        项目        供应商      工序    金额   状态│ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ OC-2501-0015   XX产线      精密机加工   机加工  ¥12,500   │ │
│  │ ├─ 5种零件 共20件   要求交期: 01-12   ⚠ 黄色预警         │ │
│  │ └─ 进度: ████████░░ 80%                    [生产中]      │ │
│  │                                                [跟踪] [详情]│ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ OC-2501-0014   YY设备      钣金加工厂   钣金    ¥8,200    │ │
│  │ ├─ 3种零件 共15件   要求交期: 01-10                       │ │
│  │ └─ 已发货: 01-09  物流单号: SF1234567    [待检验]        │ │
│  │                                                [检验] [详情]│ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ OC-2501-0013   ZZ检测      阳极氧化厂   表处理  ¥3,500    │ │
│  │ ├─ 2种零件 共50件   要求交期: 01-15                       │ │
│  │ └─ 已确认: 预计01-13发货                  [生产中]        │ │
│  │                                                [跟踪] [详情]│ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  共 45 条记录   本月外协金额: ¥156,800      < 1 2 3 ... 5 >    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 创建外协订单

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 返回   新建外协订单                                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 基本信息                                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 项目:     [XX自动化产线项目               ▼]             │   │
│  │ 工序类型: [机加工                         ▼]             │   │
│  │ 供应商:   [精密机加工厂                   ▼] [推荐供应商]│   │
│  │ 要求交期: [2025-01-15                     📅]            │   │
│  │ 优先级:   ○ 普通  ○ 紧急  ○ 加急                        │   │
│  │ 是否发料: ☐ 需要提供原材料                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 外协明细                        [从BOM导入] [添加行]     │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 序号│ 零件编码    │ 零件名称    │ 图号   │数量│单价 │金额│   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  1  │ P-MC-001   │ 底板        │D001-01│ 2  │500  │1000│   │
│  │     │ 材质: AL6061  规格: 300x200x20                     │   │
│  │     │ 📎 D001-01_V2.pdf                         [删除]  │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  2  │ P-MC-002   │ 侧板        │D001-02│ 4  │300  │1200│   │
│  │     │ 材质: AL6061  规格: 150x100x15                     │   │
│  │     │ 📎 D001-02_V1.pdf                         [删除]  │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │  3  │ P-MC-003   │ 定位块      │D001-03│ 8  │150  │1200│   │
│  │     │ 材质: S45C  规格: 50x30x20                         │   │
│  │     │ 📎 未上传图纸                      [上传]  [删除]  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 金额汇总                                                 │   │
│  │ 小计: ¥3,400.00   税率: 13%   税额: ¥442.00             │   │
│  │ 价税合计: ¥3,842.00                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 备注: [加工要求：公差按图纸，表面粗糙度Ra1.6            ]│   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │               [保存草稿]        [提交订单]               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 进度跟踪看板

```
┌─────────────────────────────────────────────────────────────────┐
│  外协进度跟踪                                   [刷新] [导出]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 项目筛选: [全部项目              ▼]   显示: [本周交期 ▼]  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      进度概览                            │   │
│  │  生产中: 12单  待发货: 3单  已发货: 5单  待检验: 4单     │   │
│  │  ⚠ 预警: 2单                                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 交期: 01-10 (今天)                                       │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 🔴 OC-2501-0010 精密机加工厂  进度70%  延期风险高        │   │
│  │    XX产线-底座零件(5件)                                  │   │
│  │    最新进度: 01-09 15:30 已完成粗加工，精加工中          │   │
│  │    [催单] [查看详情]                                     │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 🟢 OC-2501-0011 钣金加工厂    已发货                     │   │
│  │    YY设备-外壳钣金(3件)                                  │   │
│  │    发货: 01-09  物流: SF1234567  预计今日到达            │   │
│  │    [确认收货]                                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 交期: 01-12                                              │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 🟡 OC-2501-0015 精密机加工厂  进度80%  黄色预警          │   │
│  │    XX产线-传动零件(10件)                                 │   │
│  │    最新进度: 01-10 09:00 精加工中，预计01-11完成         │   │
│  │    [查看详情]                                            │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 🟢 OC-2501-0016 表面处理厂    进度100% 待发货            │   │
│  │    ZZ检测-铝件阳极(20件)                                 │   │
│  │    最新进度: 01-10 10:00 阳极完成，待发货                │   │
│  │    [查看详情]                                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.4 来料检验页面

```
┌─────────────────────────────────────────────────────────────────┐
│  来料检验  IQC-2501-0008                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 收货信息                                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 外协单号: OC-2501-0011        供应商: 钣金加工厂         │   │
│  │ 收货日期: 2025-01-10          收货数量: 15件             │   │
│  │ 零件名称: 外壳钣金件          图号: D002-01              │   │
│  │ 批次号:   BC20250110-001                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 检验方式: ● 抽检(5件)  ○ 全检  ○ 免检                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 检验项目                                                  │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 1. 外形尺寸 L                                             │ │
│  │    标准: 500±0.5mm   实测: [500.2    ]mm   ● 合格 ○ 不合格│ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 2. 外形尺寸 W                                             │ │
│  │    标准: 300±0.5mm   实测: [299.8    ]mm   ● 合格 ○ 不合格│ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 3. 折弯角度                                               │ │
│  │    标准: 90°±0.5°    实测: [90.2     ]°    ● 合格 ○ 不合格│ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 4. 表面质量                                               │ │
│  │    标准: 无划伤、变形  结果: [良好    ]     ● 合格 ○ 不合格│ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ 5. 孔位尺寸                                               │ │
│  │    标准: Φ10+0.02    实测: [10.01    ]mm   ● 合格 ○ 不合格│ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 检验结论: ● 合格  ○ 不合格  ○ 有条件接收                │   │
│  │ 合格数量: [15     ]件   不合格数量: [0      ]件          │   │
│  │ 检验备注: [                                        ]     │   │
│  │ 缺陷照片: [+ 上传照片]                                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 不合格处理: ○ 退货  ○ 返工  ○ 让步接收                  │   │
│  │ 处理说明:   [                                        ]   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │        [保存草稿]   [提交检验结果]   [合格入库]          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.5 供应商门户

```
┌─────────────────────────────────────────────────────────────────┐
│  ████ 供应商协作平台                          精密机加工厂 [退出]│
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📋 待处理事项                                            │   │
│  │ ┌─────────────────────────────────────────────────────┐ │   │
│  │ │ 🔔 2个新订单待确认    🔔 1个对账单待确认             │ │   │
│  │ └─────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 订单列表                              [待确认] [生产中]  │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 🆕 OC-2501-0020   5种零件/共25件                        │   │
│  │    要求交期: 2025-01-18   金额: ¥8,500                  │   │
│  │    [查看详情] [确认订单]                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 🆕 OC-2501-0019   3种零件/共10件                        │   │
│  │    要求交期: 2025-01-15   金额: ¥4,200                  │   │
│  │    [查看详情] [确认订单]                                 │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 📦 OC-2501-0015   5种零件/共20件      进度 80%          │   │
│  │    确认交期: 2025-01-12   金额: ¥12,500                 │   │
│  │    [更新进度] [发货通知]                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 更新进度  OC-2501-0015                            [×]   │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 当前进度: [精加工中                             ▼]      │   │
│  │ 完成比例: [80        ]%                                 │   │
│  │ 已完成数: [16        ]件                                │   │
│  │ 预计发货: [2025-01-11                          📅]      │   │
│  │ 进度说明: [正在进行最后4件的精加工，预计明日完成   ]    │   │
│  │ 附件:     [+ 上传进度照片]                              │   │
│  │           📷 进度照片1.jpg  📷 进度照片2.jpg            │   │
│  │                                                          │   │
│  │              [取消]                [提交进度]            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 权限设计

### 7.1 功能权限矩阵

| 功能 | 采购专员 | 采购主管 | QA | PM | 供应商 |
|------|:--------:|:--------:|:--:|:--:|:------:|
| 查看外协单 | ✓ | ✓ | ✓ | ✓ | ○ |
| 创建外协单 | ✓ | ✓ | ✗ | ✓ | ✗ |
| 编辑外协单 | ✓ | ✓ | ✗ | ✗ | ✗ |
| 删除外协单 | ✗ | ✓ | ✗ | ✗ | ✗ |
| 审批外协单 | ✗ | ✓ | ✗ | ✗ | ✗ |
| 发送订单 | ✓ | ✓ | ✗ | ✗ | ✗ |
| 跟踪进度 | ✓ | ✓ | ✗ | ✓ | ✗ |
| 更新进度 | ✓ | ✓ | ✗ | ✗ | ✓ |
| 收货登记 | ✓ | ✓ | ✗ | ✗ | ✗ |
| 来料检验 | ✗ | ✗ | ✓ | ✗ | ✗ |
| 管理供应商 | ✓ | ✓ | ✗ | ✗ | ✗ |
| 供应商评价 | ✓ | ✓ | ✓ | ✗ | ✗ |
| 结算对账 | ✓ | ✓ | ✗ | ✗ | ○ |

### 7.2 数据权限

| 角色 | 数据范围 |
|------|----------|
| 采购专员 | 本人创建的外协单 |
| 采购主管 | 所有外协单 |
| PM | 本项目外协单 |
| QA | 所有待检验记录 |
| 供应商 | 本供应商相关订单 |

---

## 8. 集成与通知

### 8.1 模块集成

```
┌─────────────────────────────────────────────────────────────────┐
│                      外协模块集成关系                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  上游模块                         下游模块                       │
│  ─────────                       ─────────                      │
│                                                                 │
│  项目管理 ─────────┐       ┌────→ 项目进度                       │
│  (项目信息)        │       │      (关键路径)                     │
│                    │       │                                    │
│  BOM管理 ──────────┼──→ 外协 ──→ 库存管理                        │
│  (零件清单)        │  管理  │      (入库)                        │
│                    │  模块  │                                    │
│  设计变更(ECN) ────┘       ├────→ 财务模块                       │
│  (图纸更新)                │      (应付账款)                     │
│                            │                                    │
│                            └────→ 质量管理                       │
│                                   (IQC记录)                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 通知配置

```yaml
外协相关通知:
  order_pending_approval:
    title: "外协订单待审批"
    template: "外协单 {order_no} 待审批，金额 {amount}"
    recipients: [pu_manager]

  order_sent_to_supplier:
    title: "新订单通知"
    template: "您有新的外协订单 {order_no}，请及时确认"
    recipients: [supplier]
    channels: [email, sms]

  order_confirmed:
    title: "订单已确认"
    template: "外协单 {order_no} 已被供应商确认，预计交期 {confirmed_date}"
    recipients: [purchaser, pm]

  delivery_warning_yellow:
    title: "交期黄色预警"
    template: "外协单 {order_no} 距交期仅剩3天，当前进度{progress}%"
    recipients: [purchaser]

  delivery_warning_red:
    title: "交期红色预警"
    template: "外协单 {order_no} 已超期{days}天，请立即跟进"
    recipients: [purchaser, pu_manager, pm]

  goods_received:
    title: "外协件已到货"
    template: "外协单 {order_no} 已到货，共{qty}件，请安排检验"
    recipients: [qa_team]

  iqc_completed:
    title: "检验完成"
    template: "外协单 {order_no} 检验完成，结果：{result}"
    recipients: [purchaser, pm]

  iqc_rejected:
    title: "检验不合格"
    template: "外协单 {order_no} 检验不合格，不合格{qty}件，请处理"
    recipients: [purchaser, pu_manager, supplier]
    channels: [system, email]
```

---

## 9. 配置项

### 9.1 系统配置

| 配置项 | 配置键 | 默认值 | 说明 |
|--------|--------|--------|------|
| 免审批金额上限 | outsourcing.approval.threshold | 5000 | 金额(元) |
| 二级审批金额 | outsourcing.approval.level2_threshold | 20000 | 金额(元) |
| 交期预警天数(黄) | outsourcing.warning.yellow_days | 3 | 天 |
| 交期预警天数(红) | outsourcing.warning.red_days | 0 | 天 |
| 默认税率 | outsourcing.default_tax_rate | 13 | 百分比 |
| 抽检比例 | outsourcing.iqc.sampling_ratio | 20 | 百分比 |
| 结算周期 | outsourcing.settlement.period | MONTHLY | 月结 |
| 供应商门户有效期 | outsourcing.portal.token_expiry_hours | 24 | 小时 |

### 9.2 工序类型配置

```yaml
process_types:
  MACHINING:
    name: "机加工"
    default_lead_time: 5
    inspection_items:
      - "外形尺寸"
      - "孔位尺寸"
      - "表面粗糙度"
      - "形位公差"

  SHEET_METAL:
    name: "钣金加工"
    default_lead_time: 3
    inspection_items:
      - "外形尺寸"
      - "折弯角度"
      - "焊接质量"
      - "表面质量"

  SURFACE:
    name: "表面处理"
    default_lead_time: 3
    inspection_items:
      - "膜厚"
      - "颜色"
      - "附着力"
      - "外观"

  HEAT_TREAT:
    name: "热处理"
    default_lead_time: 3
    inspection_items:
      - "硬度"
      - "金相组织"
      - "变形量"

  WIRE_CUT:
    name: "线切割"
    default_lead_time: 3
    inspection_items:
      - "轮廓尺寸"
      - "表面粗糙度"
      - "垂直度"
```

---

## 10. 附录

### 10.1 外协订单状态对照

| 状态码 | 中文名称 | 英文名称 | 说明 |
|--------|----------|----------|------|
| DRAFT | 草稿 | Draft | 订单创建中 |
| PENDING_APPROVAL | 待审批 | Pending Approval | 等待审批 |
| CONFIRMED | 已确认 | Confirmed | 审批通过，待发送 |
| SENT | 已发送 | Sent | 已发送供应商 |
| IN_PRODUCTION | 生产中 | In Production | 供应商确认后 |
| SHIPPED | 已发货 | Shipped | 供应商已发货 |
| DELAYED | 已延期 | Delayed | 超过交期未发货 |
| RECEIVED | 已收货 | Received | 仓库已收货 |
| PASSED | 检验合格 | Passed | IQC通过 |
| REJECTED | 检验不合格 | Rejected | IQC不通过 |
| REWORKING | 返工中 | Reworking | 不合格品返工 |
| COMPLETED | 已完成 | Completed | 订单完成 |
| CANCELLED | 已取消 | Cancelled | 订单取消 |

### 10.2 供应商评分标准

| 评分维度 | 评分标准 | 权重 |
|----------|----------|------|
| 质量评分 | 合格率100%=5分，每降1%扣0.2分 | 40% |
| 交期评分 | 按时交付=5分，延期1天扣0.5分 | 35% |
| 服务评分 | 响应及时性、配合度、问题处理 | 15% |
| 价格评分 | 与市场价对比的合理性 | 10% |

### 10.3 常见缺陷类型

| 缺陷类型 | 代码 | 说明 |
|----------|------|------|
| 尺寸超差 | DIMENSION | 尺寸不符合图纸要求 |
| 表面缺陷 | SURFACE | 划伤、凹坑、色差等 |
| 形位超差 | GEOMETRIC | 平面度、垂直度、同轴度等 |
| 材质问题 | MATERIAL | 材质不符、硬度不够等 |
| 加工遗漏 | MISSING | 漏加工、漏钻孔等 |
| 数量短缺 | QUANTITY | 交货数量不足 |

---

## 版本历史

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| v1.0 | 2025-07-12 | System Architect | 初始版本 |

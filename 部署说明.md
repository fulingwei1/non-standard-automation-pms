# 🚀 快速部署指南

## 一、部署前准备

### 1. 启动 Docker Desktop
- 双击打开 Docker Desktop 应用
- 等待 Docker 图标变为绿色（表示已就绪）
- 如未安装，请从 https://www.docker.com/products/docker-desktop/ 下载

### 2. 确认 Docker 运行状态
```bash
docker --version
docker ps
```
如果看到版本号和进程列表，说明 Docker 正常运行。

## 二、一键部署（推荐）

在项目根目录执行：

```bash
./deploy.sh
```

该脚本会自动完成：
- ✅ 检查 Docker 环境
- ✅ 构建前端项目
- ✅ 创建 Docker 镜像
- ✅ 启动所有服务（前端+后端+MySQL+Redis）
- ✅ 运行健康检查

## 三、访问系统

部署成功后，打开浏览器访问：

### 主要地址
- **系统首页**: http://localhost
- **API文档**: http://localhost/docs
- **健康检查**: http://localhost/health

### 默认登录账号
- 用户名: `admin`
- 密码: `admin123`

⚠️ **首次登录后请立即修改密码！**

## 四、常用管理命令

### 查看服务状态
```bash
docker compose -f docker-compose.production.yml ps
```

### 查看实时日志
```bash
# 所有服务
docker compose -f docker-compose.production.yml logs -f

# 只看应用日志
docker compose -f docker-compose.production.yml logs -f app
```

### 停止服务
```bash
docker compose -f docker-compose.production.yml down
```

### 重启服务
```bash
docker compose -f docker-compose.production.yml restart
```

## 五、服务说明

部署包含以下服务：

| 服务 | 端口 | 说明 |
|------|------|------|
| **前端 + 后端** | 80 | Nginx + FastAPI |
| **MySQL** | 3306 | 数据库 |
| **Redis** | 6379 | 缓存 |

## 六、常见问题

### ❌ Docker 未运行
**错误**: Cannot connect to the Docker daemon

**解决**: 启动 Docker Desktop 应用，等待就绪后重试

### ❌ 端口被占用
**错误**: port is already allocated

**解决**: 
```bash
# 查看占用进程
lsof -i :80

# 停止该进程或修改端口配置
```

### ❌ 前端打不开
**检查**:
```bash
# 1. 确认服务运行
docker compose -f docker-compose.production.yml ps

# 2. 查看日志
docker compose -f docker-compose.production.yml logs app

# 3. 测试健康检查
curl http://localhost/health
```

### ❌ 内存不足
**解决**: 
- 打开 Docker Desktop → Settings → Resources
- 增加 Memory 到至少 4GB

## 七、完整部署文档

详细配置、优化和故障排查请参考：
- **完整文档**: `DEPLOYMENT_GUIDE.md`
- **README**: `README.md`

## 八、技术支持

需要帮助时，请提供：
```bash
# 1. Docker 版本
docker --version

# 2. 服务状态
docker compose -f docker-compose.production.yml ps

# 3. 最近日志
docker compose -f docker-compose.production.yml logs --tail=50
```

---

## 🎉 部署成功！

访问 http://localhost 开始使用系统
